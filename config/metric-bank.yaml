schema: ledger.metric-bank/1

# =============================================================================
# THE METRIC BANK
# =============================================================================
#
# This file defines what a value IS. It holds no decision power whatsoever.
#
# There are no thresholds here, no tiers, no pass/fail, no good/bad, and no
# reference to any profile. Every entry below would read identically for a user
# who runs one profile and for a user who invents ten more.
#
# Where a level appears in this file it is only ever an APPLICABILITY test —
# a statement that the number does not mean anything for this company, not a
# judgement about what to do when it doesn't. What a profile does with a
# non-meaningful value is the profile's business.
#
# The reasoning for any specific level ("15% because...") lives in the profile
# that chose that level, never here.
#
# -----------------------------------------------------------------------------
# Conventions
# -----------------------------------------------------------------------------
#
# kind
#   computed      derived from filings, prices, or other entries
#   qualitative   not computed; the user writes prose and marks it
#
# estimator       HOW the value is read, which is not the same as what it
#                 measures. `kind` names one of the host's estimator kinds
#                 (engine/contract.py, ESTIMATORS) and `observations` says how
#                 many annual observations it reads, where the estimator reads
#                 a window of fixed length.
#
#                 It is here because it is a property of the measure and of
#                 nothing else — the same fact as `unit`, and as intrinsic as
#                 `polarity`. It is NOT a threshold and NOT a judgement: it
#                 says what one more filing can do to this reading, which is
#                 arithmetic. What follows from it is how much evidence a
#                 breach of a level needs, and the host derives that so an
#                 author cannot get it wrong on a new measure — see the
#                 comment above ESTIMATORS for why that stopped being a
#                 setting a strategy states.
#
#                 Where a measure combines legs read differently, the kind
#                 names the noisiest one: the leg a new filing replaces. A
#                 price divided by a three-year average earnings is `instant`,
#                 because the price is a new number every day and the average
#                 is not.
#
# polarity        Which direction is favourable, as an intrinsic property of
#                 the measure. NEVER a statement about any particular level.
#   higher_is_better
#   lower_is_better
#   none          The measure has no intrinsically favourable direction.
#                 Several entries below are deliberately `none` because the
#                 profiles that use them read the same number in opposite
#                 directions. That disagreement belongs to them, not here.
#
# unit            percent | percentage_points | times | ratio | score | usd |
#                 shares | years | count | times_own_median | yes_no
#
#                 yes_no belongs to the qualitative entries: what they carry
#                 is a mark, and a mark reads as an answer rather than as a
#                 quantity. Never a `format` beside it — a format is for
#                 numbers, and running a yes/no through one prints a 1.
#
# inputs
#   filings       raw figures from SEC XBRL company facts
#   prices        daily price data
#   entries       other entries in this bank
#
# parameters      A value the computation needs that this file does not fix —
#                 a rate, a multiple, a period. The formula stays here and is
#                 identical for every profile; the profile supplies the number.
#                 A parameter is an INPUT, never a threshold.
#
# not_meaningful_when
#                 Conditions under which the value does not mean anything for
#                 this company. Two forms, and the difference is WHO REFUSES:
#
#                   industry: [class ids]
#                       The HOST refuses, before the formula runs, from the
#                       industry code the SEC publishes for the filer. Class
#                       ids come from engine/contract.py INDUSTRY_CLASSES and
#                       nowhere else; an unknown one fails the load. The
#                       measure reports `inapplicable` — permanently, and
#                       distinctly from a figure that is merely missing.
#
#                   data: <prose>
#                       The FORMULA refuses, from the figures themselves — a
#                       negative denominator, a base too small to compound
#                       from, a window that does not align. Stated here so a
#                       reader can find out why a measure reads absent;
#                       enforced in engine/compute.py, where the arithmetic
#                       that can see it is.
#
#                   undetected: <prose>   (plus `needs`)
#                       NOBODY refuses. The condition is real and nothing here
#                       can tell whether it holds. One case exists — a
#                       consolidated captive finance arm, whose filer is an
#                       automaker or an equipment maker and therefore sits
#                       nowhere near the codes this host can read. `needs`
#                       says what it would take to settle it, so the gap is a
#                       standing request rather than a shrug, and the screen
#                       tells the reader in as many words that this one is
#                       theirs to check.
#
#                 Every item carries `because`: plain language saying why the
#                 measure means nothing there. A refusal a reader cannot
#                 understand teaches them the tool is broken.
#
#                 These conditions sat here as prose for as long as this file
#                 existed, flagged untestable, and nothing ever read them: a
#                 lender's debt-to-equity read 1.4x while its assets were
#                 seven times its equity, because deposits are not debt, and
#                 it passed every test put to it. Prose nothing reads is a
#                 note about a condition, not a condition. So the industry
#                 form is a key, `data` prose naming a kind of company is
#                 refused at load, and an entry built on a gated entry must be
#                 gated at least as widely — the distance between a number and
#                 a meaningless one is meaningless.
#
#                 What does NOT belong here: a threshold that reads
#                 differently for this kind of company. That is a judgement,
#                 it belongs to a strategy, and a number that is arithmetically
#                 right and merely read differently is a caution for the
#                 reader — `explanation.misfires` — not a refusal.
#
# =============================================================================


entries:

  # ===========================================================================
  # Returns on capital
  # ===========================================================================

  - id: roic_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Return on invested capital, 5-year median
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        ROIC(year) = EBIT × (1 − effective tax rate)
                     ÷ (total debt + total equity − cash & equivalents)

        value = median( ROIC(year) for each of the last five fiscal years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - EBIT
        - income tax expense
        - pre-tax income
        - short-term debt
        - long-term debt
        - total shareholders' equity
        - cash and cash equivalents
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        How many cents of profit the business earns per dollar that's tied up
        in it. A company earning 20 cents on every dollar of capital is a
        machine; one earning 7 cents is a job.
      misfires: |
        A company that just closed a large acquisition carries fresh goodwill
        in the denominator and will understate ROIC for a year or two.
        Post-ASC 842, operating lease assets sit on the balance sheet and
        depress ROIC for retailers and restaurants relative to their pre-2019
        history.
      attribution: |
        Buffett, unambiguously. He'd phrase it as return on unleveraged net
        tangible assets, which is stricter than this (it strips goodwill).
        This version is the common approximation.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Return on invested capital divides profit by the money tied up in
          the business — borrowing plus equity, less cash. For a lender that
          sum is the funding it exists to lend out rather than capital tied
          up, and the deposits which are most of it are not counted as
          borrowing at all, so the divisor is a small fraction of the real one
          and the return comes out high. An insurer's investment portfolio is
          held against claims not yet made, so a return measured against it is
          measuring money that belongs to policyholders.
      - industry: [real-estate]
        because: |
          The profit on top is struck after depreciation on buildings, which
          is an accounting convention rather than a cost being incurred, so it
          understates by design — and the capital underneath is buildings at
          what they cost decades ago rather than what they are worth. Both
          ends are wrong and they do not cancel.
      - data: invested capital (the denominator) is under 10% of revenue
        because: |
          Asset-light companies can have near-zero or negative invested
          capital, which makes the ratio explode or invert into nonsense.

          Revenue is what the denominator is judged small against, which is
          why it is listed as an input even though the formula above never
          uses it. Where the filer's revenue cannot be read over the same
          fiscal years, this condition cannot be settled either way — and a
          test that cannot be run has not been passed, so the measure is
          absent rather than computed ungated. It used to be skipped
          silently, which meant the one class of filer most likely to be
          missing a revenue total was the one class computing this without
          the guard the guard exists for.

  - id: roe_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Return on equity, 5-year median
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        ROE(year) = net income ÷ average shareholders' equity

        value = median( ROE(year) for each of the last five fiscal years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - net income
        - total shareholders' equity
      prices: []
      entries: []
    explanation:
      plain: |
        Profit earned per dollar of shareholder money left in the business.
      misfires: |
        Heavy goodwill inflates equity and depresses ROE for acquirers. Any
        leveraged business will look better on this than it deserves, because
        borrowing money shrinks the denominator without improving the business.
      attribution: |
        Buffett. His most-cited single figure through the 1970s and 1980s
        letters.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          Both halves break, in opposite directions. The profit on top is
          after depreciation on buildings that mostly are not wearing out, so
          it is understated. The shareholder money underneath is those
          buildings at historic cost less all the depreciation ever charged,
          so it is understated too — and by more, because a property company
          also pays out most of its earnings by law, which leaves the
          accumulated-profit line negative rather than merely small.
      - data: total shareholders' equity is zero or negative
        because: |
          Companies that have bought back so much stock that equity is
          negative produce a negative or absurd denominator.

  - id: roe_minus_roic_gap_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Return on equity above return on invested capital, 5-year
    unit: percentage_points
    format: "+0.0"
    polarity: lower_is_better
    derivation:
      formula: |
        median(ROE over five fiscal years)
        − median(ROIC over five fiscal years)
      window: five fiscal years, annual observations, the difference of two
        medians
    inputs:
      filings: []
      prices: []
      entries:
        - roe_median_5y
        - roic_median_5y
    explanation:
      plain: |
        Return on equity counts only the owners' money. Return on invested
        capital counts the borrowed money too. So the distance between them is
        borrowing, and nothing else: a company earning 30% on its owners'
        money and 12% on all the money in the business is producing most of
        that 30% with debt rather than with the business.

        A wide gap is not a fraud and not always a mistake. It is a statement
        that the returns being admired belong partly to the lenders, and that
        they get thinner the moment borrowing gets dearer or harder.
      misfires: |
        A company with very little equity left — after years of buybacks, or
        after a large write-off — has a small denominator under return on
        equity, so the gap widens for a reason that is arithmetic rather than
        a change in how the business is funded. A company with no debt at all
        shows a small gap, correctly, but the two measures then differ only by
        the cash on the balance sheet and the tax treatment, so the number
        stops carrying much.
      attribution: |
        The construction is standard practice rather than any one investor's.
        What it formalises is the observation Buffett has made repeatedly:
        return on equity is trivially inflated by borrowing, so a company that
        levers up to defend a falling return keeps that measure looking well
        right through the deterioration.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          This measures the distance between two returns, and for these
          companies at least one of them does not mean anything — so neither
          does the distance. The gap is meant to say how much of a return
          comes from borrowing; here it would be saying how much comes from a
          number that was never describing this business.
      - data: either side is not meaningful for this company
        because: |
          The distance between a number and a meaningless one is meaningless.
          Where return on equity or return on invested capital cannot be
          computed, so is the gap.

  # ===========================================================================
  # Leverage and solvency
  # ===========================================================================

  - id: total_debt_to_ebitda
    kind: computed
    estimator:
      kind: instant
    label: Total debt to EBITDA
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        (short-term debt + long-term debt + finance lease obligations)
        ÷ EBITDA (TTM)
      window: trailing twelve months
    inputs:
      filings:
        - short-term debt
        - long-term debt
        - finance lease obligations
        - EBITDA
      prices: []
      entries: []
    explanation:
      plain: |
        How many years of earnings it would take to repay everything the
        company owes. Two years is comfortable. Five is a company whose future
        belongs partly to its lenders.
      misfires: |
        Regulated utilities and pipelines run at 4–6x as a matter of course and
        that's fine, because the revenue is legislated. REITs, the same.
      attribution: |
        Buffett's stated preference is a business that could pay off its debt
        out of a couple of years of earnings.
    not_meaningful_when:
      - industry: [depository-lending]
        because: |
          The borrowing on top is the wrong quantity, and not by a little.
          Deposits are most of what a bank owes and the whole of what it lends
          out, and no filer tags them as debt — so this counts the wholesale
          funding and misses the rest. Years-to-repay against a fraction of
          what a company owes is not a cautious reading of its leverage; it is
          a confident reading of a different company's.
      - undetected: the company has a captive finance arm
        because: |
          A company that lends to its own customers — a car maker with a
          finance arm, an equipment maker with a leasing book — carries
          borrowing that funds that loan book rather than the factory. The
          consolidated figure adds the two together and reads as though the
          industrial business were far more indebted than it is.
        needs: |
          A figure for the finance subsidiary's receivables and its own
          borrowing. Those sit in segment disclosures tagged against a
          dimension, and this program reads only undimensioned facts — so
          nothing it holds can separate the two businesses. Until it can,
          check the filing yourself where a company you are looking at lends
          to its own customers.

  - id: total_debt_to_avg_fcf_5y
    kind: computed
    estimator:
      kind: instant
    label: Total debt to average free cash flow, 5-year
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        (short-term debt + long-term debt + finance lease obligations)
        ÷ mean( free cash flow for each of the last five fiscal years )

        where free cash flow = cash from operations − capital expenditure
      window: debt at the latest balance-sheet date, against five fiscal
        years of free cash flow averaged
    inputs:
      filings:
        - short-term debt
        - long-term debt
        - finance lease obligations
        - cash from operating activities
        - capital expenditure
      prices: []
      entries: []
    explanation:
      plain: |
        How many years of the cash the business actually generates it would
        take to repay everything it owes. At 3 it would take three years of
        every spare dollar.

        It asks the same question as debt against EBITDA and answers it with a
        number the company cannot present its way out of. EBITDA adds back
        depreciation, which is the cost of the equipment wearing out;
        free cash flow has already paid for replacing it. For a business that
        needs a lot of machinery, the two figures can differ by half.
      misfires: |
        A company in the middle of a deliberate expansion — building
        factories, laying fibre — has low free cash flow by choice rather than
        by weakness, and reads as more indebted than it is. Averaging five
        years absorbs one such year and not a programme of them.

        The debt is a single date and the cash flow is five years, so a
        company that borrowed heavily last quarter shows the new debt against
        cash flows earned before it was spent.
      attribution: |
        The construction is standard credit practice. It is here because of a
        specific objection: Munger's position on EBITDA is that the word
        should be read as a synonym for fictitious earnings, and Buffett has
        written repeatedly that depreciation is a real expense and that any
        measure ignoring it flatters the capital-hungry.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          Dividing borrowing by that inflated figure says the debt could be
          repaid in a fraction of the years it really would take, and the more
          trouble the lender is in the better it reads.
      - data: average free cash flow over the five years is zero or negative
        because: |
          There is no number of years of a negative figure that repays a debt.
          The company is consuming cash rather than producing it, which is a
          more serious statement than any ratio, and a ratio would hide it.
      - undetected: the company has a captive finance arm
        because: |
          As with debt against operating earnings: the consolidated borrowing
          funds a loan book rather than operations, and the two cannot be told
          apart from what this program reads.
        needs: |
          A figure for the finance subsidiary's receivables and its own
          borrowing. Those sit in segment disclosures tagged against a
          dimension, and this program reads only undimensioned facts — so
          nothing it holds can separate the two businesses. Until it can,
          check the filing yourself where a company you are looking at lends
          to its own customers.

  - id: net_debt_to_ebitda
    kind: computed
    estimator:
      kind: instant
    label: Net debt to EBITDA
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        (total debt − cash & equivalents) ÷ EBITDA (TTM)
      window: trailing twelve months
    inputs:
      filings:
        - short-term debt
        - long-term debt
        - cash and cash equivalents
        - EBITDA
      prices: []
      entries: []
    explanation:
      plain: |
        The same question as total debt to EBITDA, but crediting the company
        for the cash it already holds: how many years of earnings it would take
        to repay what it owes after spending its own bank balance first.
      misfires: |
        Inherits the captive-finance and regulated-utility distortions of the
        gross measure. Netting cash also flatters a company whose cash is
        committed — trapped offshore, pledged, or already earmarked for an
        announced acquisition.
      attribution: |
        Standard credit analysis. Used here as the net counterpart to
        [total_debt_to_ebitda].
    not_meaningful_when:
      - industry: [depository-lending]
        because: |
          The same break as the gross measure, and one more on top.
          Subtracting cash from a borrowing figure that already leaves out
          deposits gives a lender a large negative net debt — as though it
          were sitting on spare money — when the cash is reserves it is
          required to hold at the central bank and cannot lend or spend.
      - undetected: the company has a captive finance arm
        because: |
          Same reason as the gross measure: consolidated debt funds a loan
          book rather than operations, and the cash held against those
          obligations is netted off as though it were spare.
        needs: |
          A figure for the finance subsidiary's receivables and its own
          borrowing. Those sit in segment disclosures tagged against a
          dimension, and this program reads only undimensioned facts — so
          nothing it holds can separate the two businesses. Until it can,
          check the filing yourself where a company you are looking at lends
          to its own customers.

  - id: debt_to_equity
    kind: computed
    estimator:
      kind: instant
    label: Debt to equity
    unit: ratio
    format: "0.00"
    polarity: lower_is_better
    derivation:
      formula: |
        total debt ÷ total shareholders' equity
      window: most recent balance sheet
    inputs:
      filings:
        - short-term debt
        - long-term debt
        - total shareholders' equity
      prices: []
      entries: []
    explanation:
      plain: |
        How much of the company is financed by lenders versus by its owners.
        At 1.0 there is a dollar of debt for every dollar of shareholder money.
      misfires: |
        Recently-IPO'd companies with large cash balances and small equity
        bases can read oddly.
      attribution: |
        Graham's balance-sheet tests and Lynch's, both. Standard.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          This claims to say how much of the company is financed by lenders
          rather than by its owners, and for a bank it does not: deposits are
          money the owners did not put in and the figure on top excludes them
          entirely. A deposit-funded lender whose assets are seven times its
          equity can read a comfortable 1.4 here. An insurer's is the same
          shape — what it owes is claims not yet made, which are not tagged as
          borrowing either.
      - data: total shareholders' equity is zero or negative
        because: |
          The ratio has no interpretation with a zero or negative denominator.

  - id: interest_coverage
    kind: computed
    estimator:
      kind: trailing
    label: Interest coverage
    unit: times
    format: "0.0x"
    polarity: higher_is_better
    derivation:
      formula: |
        EBIT ÷ interest expense (TTM)
      window: trailing twelve months
    inputs:
      filings:
        - EBIT
        - interest expense
      prices: []
      entries: []
    explanation:
      plain: |
        How many times over the company's operating profit covers its interest
        bill. At 8x, earnings could fall by half and the lenders still get paid
        without anyone noticing.
      misfires: |
        Companies capitalising interest into construction projects understate
        the expense, which flatters the ratio.

        A company with almost no borrowing reads absurdly high — a lease
        interest line of a quarter of a million against three billion of
        operating profit is twelve thousand times over. Nothing is wrong with
        the arithmetic and nothing is wrong with the company; the ratio is
        simply answering a question that does not apply to a business with no
        interest bill to cover. Read it as "no meaningful borrowing" rather
        than as a safety margin.

        For a lender, interest is closer to a cost of goods than to a
        financing choice, so a figure that would look alarming for a
        manufacturer can be ordinary here. Read it against what lenders earn
        rather than against a bond-safety standard written for industrials.
        This is a caution and not a refusal, and the reason is worth stating:
        most lenders strike no operating-profit subtotal at all, so the
        measure is simply absent for them; where one does strike it, whether
        it sits before or after funding interest is that filer's own
        presentation, and two lenders under one industry code present it both
        ways. A code that cannot separate them is not the predicate for this.
      attribution: |
        Graham's bond-safety standard, in its original form.
    not_meaningful_when:
      - data: interest expense is zero or negative
        because: |
          There is nothing to divide by. A company with no interest bill has
          no coverage of it to state, and a negative one — net interest
          income tagged into the expense line — would invert the ratio so
          that the more the company earned, the worse its coverage read.

          This said "zero or near zero" and the formula refused on zero, so
          half of it was a sentence nothing performed. It is stated as what
          is enforced, because a condition that overstates what the
          arithmetic does is how a reader comes to trust a guard that is not
          there. What "near zero" was reaching for is real and is in
          `misfires` above, where a caution belongs: a near-debt-free company
          reads absurdly high, and that reading is arithmetically correct.
          There is no honest line between "no borrowing" and "a little
          borrowing" that this bank has measured, and an unmeasured one in a
          refusal would decide against real companies on a number somebody
          picked.

  - id: ltd_to_working_capital
    kind: computed
    estimator:
      kind: instant
    label: Long-term debt to working capital
    unit: ratio
    format: "0.00"
    polarity: lower_is_better
    derivation:
      formula: |
        long-term debt ÷ (current assets − current liabilities)
      window: most recent balance sheet
    inputs:
      filings:
        - long-term debt
        - total current assets
        - total current liabilities
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company's long-term borrowings could be covered by the
        liquid cushion it already has. It ignores fixed assets entirely, which
        is deliberate: in liquidation, the factory sells for a fraction of book.
      misfires: |
        Utilities, telecom, railroads, and anything capital-intensive fail this
        permanently.
      attribution: |
        Graham's own test, stated in exactly this form. Stricter and more
        literal than debt-to-equity.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          Banks, insurers and property companies do not divide the balance
          sheet into what falls due within the year and what does not, so
          there is no working capital to read and no current ratio to strike.
          This is not a filing that is missing something: the division does
          not exist for these businesses, and it never will.
      - data: working capital (current assets − current liabilities) is zero
          or negative
        because: |
          Negative working capital makes the ratio undefined.

  - id: current_ratio
    kind: computed
    estimator:
      kind: instant
    label: Current ratio
    unit: ratio
    format: "0.00"
    polarity: none
    polarity_note: |
      Deliberately none. A low current ratio can mean a company is fragile, or
      it can mean the company collects from customers before it pays suppliers
      and is being financed for free by its own operations. The measure does not
      distinguish these and this file must not pretend to.
    derivation:
      formula: |
        total current assets ÷ total current liabilities
      window: most recent balance sheet
    inputs:
      filings:
        - total current assets
        - total current liabilities
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company holds enough short-term assets — cash, receivables,
        inventory — to cover the bills coming due within a year.
      misfires: |
        Retailers, restaurants, and subscription businesses with large deferred
        revenue routinely run below 1.0 and are fine. Negative working capital
        has been read both as a solvency problem and as evidence of competitive
        strength, depending entirely on who is reading it.
      attribution: |
        Graham, as a solvency test. Buffett has repeatedly praised negative
        working capital as a feature.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          Banks, insurers and property companies do not divide the balance
          sheet into what falls due within the year and what does not, so
          there is no working capital to read and no current ratio to strike.
          This is not a filing that is missing something: the division does
          not exist for these businesses, and it never will.

  - id: altman_z_score
    kind: computed
    estimator:
      kind: instant
    label: Altman Z-score
    unit: score
    format: "0.00"
    polarity: higher_is_better
    derivation:
      formula: |
        1.2 × (working capital / total assets)
        + 1.4 × (retained earnings / total assets)
        + 3.3 × (EBIT / total assets)
        + 0.6 × (market cap / total liabilities)
        + 1.0 × (revenue / total assets)
      window: most recent balance sheet and trailing twelve months
    inputs:
      filings:
        - total current assets
        - total current liabilities
        - retained earnings
        - total assets
        - total liabilities
        - EBIT
        - revenue
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        A bankruptcy probability score, built from five balance-sheet and
        income-statement ratios weighted by how well each predicted failure in
        the original study.
      misfires: |
        Very asset-light companies score poorly on the working capital and
        asset turnover terms for reasons unrelated to distress. Young companies
        have low retained earnings by construction.
      attribution: |
        Altman, 1968. Not Graham's, but the formalisation of what Graham's
        balance sheet tests were reaching for, with fifty years of
        out-of-sample validation behind it.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Altman built this from manufacturers and excluded financial
          companies himself, and the reason is visible in the terms: working
          capital over assets needs a balance sheet split into what falls due
          within the year and what does not, which a bank does not publish,
          and sales over assets compares a loan book against interest income.
          The score would be a confident number produced from ratios that were
          never fitted to these accounts.
      - industry: [real-estate]
        because: |
          The first of the five terms is working capital over assets, and a
          property company does not divide its balance sheet into what falls
          due within the year and what does not — so there is nothing to read
          and never will be, whatever gets filed next. The score cannot be
          struck at all. What would be left if it could is a fit made on
          manufacturers reading a company whose accumulated profit is at or
          below zero because the law requires nearly all earnings to be paid
          out, which is the form of the company rather than distress.

  - id: altman_z_double_prime
    kind: computed
    estimator:
      kind: instant
    label: Altman Z″-score, for companies that do not manufacture
    unit: score
    format: "0.00"
    polarity: higher_is_better
    derivation:
      formula: |
        6.56 × (working capital / total assets)
        + 3.26 × (retained earnings / total assets)
        + 6.72 × (EBIT / total assets)
        + 1.05 × (book equity / total liabilities)
      window: most recent balance sheet and trailing twelve months
    inputs:
      filings:
        - total current assets
        - total current liabilities
        - retained earnings
        - total assets
        - total liabilities
        - total shareholders' equity
        - EBIT
      prices: []
      entries: []
    explanation:
      plain: |
        The same bankruptcy score as above, in the version its author built
        for companies that do not make things. Two differences, and both
        matter more than the changed weights.

        The sales-to-assets term is gone. It was fitted on public
        manufacturers, where a factory turning over its assets slowly is a
        warning; a consultancy or a software company turns over very few
        assets at all and is not thereby in distress.

        And the equity term uses what the books say the equity is worth
        rather than what the market says. The score therefore does not move
        when the share price does — which is what a solvency test inside a
        valuation rule set needs, because otherwise a share falling for being
        cheap reads as a share falling for being in trouble, and the one
        measure meant to tell those apart moves with the wrong one.
      misfires: |
        The distress and safe zones are not the same numbers as the original
        score's: Altman's published bands for this variant put distress below
        1.1 and safety above 2.6, against 1.8 and 3.0 for the five-variable
        version. A level carried across from one to the other means something
        different.

        Young companies still score low on retained earnings by construction,
        having had few years in which to retain any.
      attribution: |
        Altman. The four-variable variant is his own, published for
        non-manufacturers and for companies outside the US market the original
        was fitted on.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Altman excluded financial companies from the original study and this
          variant does not change that. A bank's balance sheet is not divided
          into current and non-current at all, so the working capital term has
          nothing to read.
      - industry: [real-estate]
        because: |
          The variant was made for companies that are not manufacturers, so it
          is the one a reader might expect to work here, and it does not: it
          still opens with working capital over assets, and a property company
          publishes no split between what falls due within the year and what
          does not. It also leans much harder on accumulated profit than the
          original — which for a property company is at or below zero because
          the law requires nearly all earnings to be paid out, so the change
          that makes the variant fairer elsewhere makes it harsher here.
      - data: total liabilities are zero
        because: |
          A company that owes nothing has no denominator for the equity term —
          and no solvency question either.

  # ===========================================================================
  # Margins, cash generation and earnings quality
  # ===========================================================================

  - id: gross_margin_ttm
    kind: computed
    estimator:
      kind: trailing
    label: Gross margin, trailing twelve months
    unit: percent
    format: "0.0%"
    polarity: none
    polarity_note: |
      Deliberately none. A high gross margin and a low one describe different
      business models, not better and worse businesses — high-volume retail and
      branded software sit at opposite ends and both work.
    derivation:
      formula: |
        (revenue − cost of revenue) ÷ revenue, over the trailing twelve months
      window: trailing twelve months
    inputs:
      filings:
        - revenue
        - cost of revenue
      prices: []
      entries: []
    explanation:
      plain: |
        Out of every hundred dollars of sales, how much is left after paying
        the direct cost of what was sold — before any overhead, research or
        marketing.
      misfires: |
        Companies that changed segment reporting or restated mid-window make
        period-to-period comparison unreliable.
      attribution: |
        Standard. Defined here because several measures below are built on it.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          A gross margin is what is left of a sale after what the sold thing
          cost, and neither business has that line. What a loan costs a bank
          is the interest it pays on deposits, which is not a cost of revenue
          and is not tagged as one; what a policy costs an insurer is claims
          that have not happened yet. Where such a filer does tag a cost line,
          it is something else entirely and the margin computed from it
          describes nothing.
      - data: the company does not report cost of revenue separately
        because: |
          Some conglomerates do not break it out, so gross margin cannot be
          computed at all.

  - id: gross_margin_range_5y
    kind: computed
    estimator:
      kind: range
      observations: 5
    label: Gross margin range, 5-year
    unit: percentage_points
    format: "0.0 pp"
    polarity: lower_is_better
    polarity_note: |
      This is a dispersion measure. Less dispersion means the margin is more
      stable; it says nothing about whether the level is high or low.
    derivation:
      formula: |
        max(annual gross margin, 5 yrs) − min(annual gross margin, 5 yrs)
      window: five fiscal years, annual observations
    inputs:
      filings:
        - revenue
        - cost of revenue
      prices: []
      entries:
        - gross_margin_ttm
    explanation:
      plain: |
        Whether the company sets its own prices or the market sets them. A
        business whose gross margin sits between 61% and 63% for five years is
        telling you customers don't shop it on price. One that swings from 22%
        to 38% is telling you it sells a commodity. Pricing power shows up as a
        flat line, not a high one.
      misfires: |
        Any company that changed segment reporting, made a transformative
        acquisition, or restated within the window will show artificial range.
      attribution: |
        Buffett's pricing-power thinking, expressed as stability rather than
        level.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          The margin this measures the swing of does not exist for a lender
          or an insurer, so neither does its steadiness. What a loan costs a
          bank is the interest it pays on deposits, which is not a cost of
          revenue and is not tagged as one; what a policy costs an insurer is
          claims that have not happened yet. Where such a filer does tag a
          cost line it is something else entirely, and five years of a margin
          computed from it is five years of describing nothing.
      - data: the company does not report cost of revenue separately
        because: |
          Gross margin cannot be computed, so its range cannot be either.

  - id: gross_margin_range_relative_5y
    kind: computed
    estimator:
      kind: range
      observations: 5
    label: Gross margin range as a share of the margin, 5-year
    unit: percent
    format: "0.0%"
    polarity: lower_is_better
    polarity_note: |
      A dispersion measure, like the range it is built from. Less dispersion
      means the margin is more stable; it says nothing about whether the level
      is high or low.
    derivation:
      formula: |
        ( max(annual gross margin, 5 yrs) − min(annual gross margin, 5 yrs) )
        ÷ median(annual gross margin, 5 yrs)
      window: five fiscal years, annual observations
    inputs:
      filings:
        - revenue
        - cost of revenue
      prices: []
      entries:
        - gross_margin_ttm
    explanation:
      plain: |
        The same question as the plain range — does this company set its own
        prices, or does the market set them — asked in a way that means the
        same thing at any margin.

        A distributor earning 12% and swinging between 9% and 15% moved six
        points, and half its margin came and went. A software company earning
        81% and swinging between 78% and 84% also moved six points, and barely
        noticed. The first is a commodity business; the second sets its own
        prices. Measured in points they are identical, which is the problem.
        Measured against the margin itself, the first reads 50% and the second
        7%.
      misfires: |
        A company whose gross margin is genuinely thin — a few percent, as in
        wholesale distribution or contract manufacturing — has a small
        denominator, so ordinary movement reads as violent. That is arguably
        correct, since a one-point move really does take a large share of such
        a company's margin, but it means this measure will not distinguish
        between thin-and-stable and thin-and-erratic as finely as it does
        higher up.

        It inherits the plain range's problem too: a segment reporting change,
        a transformative acquisition or a restatement inside the window shows
        as movement that is not the business moving.
      attribution: |
        Buffett's pricing-power thinking, as with the plain range. The
        scale-free form is standard practice rather than his; absolute
        percentage points do not travel across businesses of different margin,
        and the same objection applies to any threshold stated in points.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          The margin this measures the swing of does not exist for a lender
          or an insurer, so neither does the swing, and scaling one that was
          never there against a middle year that was never there does not
          recover it. Interest paid on deposits is not a cost of revenue and
          is not tagged as one; claims not yet made are not yet a cost of
          anything.
      - data: the company does not report cost of revenue separately
        because: |
          Gross margin cannot be computed, so neither its range nor the
          relative version can be.
      - data: the middle annual gross margin is zero or negative
        because: |
          A swing measured against nothing has no scale, and measured against
          a negative it changes sign for a reason that is arithmetic rather
          than a fact about the company.

  - id: gross_margin_change_3y
    kind: computed
    estimator:
      kind: endpoint
    label: Gross margin change over three years
    unit: percentage_points
    format: "+0.0 pp"
    polarity: higher_is_better
    derivation:
      formula: |
        gross margin (TTM) − gross margin (3 fiscal years ago)
      window: trailing twelve months against the fiscal year three years back
    inputs:
      filings:
        - revenue
        - cost of revenue
      prices: []
      entries:
        - gross_margin_ttm
    explanation:
      plain: |
        Whether the growth is being bought with discounts. Rapid revenue growth
        with falling gross margin usually means the company is purchasing market
        share, and that stops working when the money runs out.
      misfires: |
        Inherits every distortion of gross margin itself: a mid-window
        acquisition, a segment reclassification or a restatement moves this
        without anything changing in the business.
      attribution: |
        Lynch. An early-warning check on the quality of growth.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          The margin whose direction this reports does not exist for a
          lender or an insurer. A bank's cost of lending is the interest it
          pays on deposits, which no filer tags as a cost of revenue, so there
          is no margin to have moved and nothing for three years of it to say
          about pricing power.
      - data: the company does not report cost of revenue separately
        because: |
          Gross margin cannot be computed, so its change cannot be either.

  - id: gross_margin_vs_3y_median
    kind: computed
    estimator:
      kind: trailing
    label: Gross margin versus its own three-year median
    unit: percentage_points
    format: "+0.0 pp"
    polarity: higher_is_better
    derivation:
      formula: |
        gross margin (TTM) − median(annual gross margin, 3 yrs)
      window: trailing twelve months against a three-year annual median
    inputs:
      filings:
        - revenue
        - cost of revenue
      prices: []
      entries:
        - gross_margin_ttm
    explanation:
      plain: |
        Whether the company is still getting its usual price for what it sells.
        This is the measure that separates cyclical weakness from structural
        decline: revenue falls in both cases, but gross margin holding up
        through a revenue decline means the company still has pricing power and
        is just selling less, while gross margin collapsing alongside revenue
        means it's discounting to hold volume — the pattern that precedes
        permanent impairment.
      misfires: |
        A three-year median is short. A company that spent all three years in a
        single unusual condition has a median describing that condition rather
        than its normal.
      attribution: |
        The report's own construction, in Graham's discount-closure tradition.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          The margin this compares against its own recent history does not
          exist for a lender or an insurer, so there is no history to compare
          it with. What such a filer pays for its product is interest on
          deposits or claims not yet made, and neither is tagged as a cost of
          revenue — so the reading and the median it is measured against would
          both be describing nothing.
      - data: the company does not report cost of revenue separately
        because: |
          Gross margin cannot be computed, so the comparison cannot be either.

  - id: fcf_ttm
    kind: computed
    estimator:
      kind: trailing
    label: Free cash flow, trailing twelve months
    unit: usd
    format: "$0,0"
    polarity: higher_is_better
    derivation:
      formula: |
        cash from operations − capital expenditures, over the trailing twelve
        months
      window: trailing twelve months
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
      prices: []
      entries: []
    explanation:
      plain: |
        The cash left over after running the business and keeping its equipment
        current. Positive means the company funds itself. Negative means it
        needs someone else's money to keep going.
      misfires: |
        Breaks where capital expenditure is really acquisitions in disguise, or
        the reverse. Companies deliberately in a heavy investment cycle show
        negative free cash flow as a choice, not a failure.
      attribution: |
        Standard. The cash counterpart to reported profit.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

  - id: fcf_margin_ttm
    kind: computed
    estimator:
      kind: trailing
    label: Free cash flow margin, trailing twelve months
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        (cash from operations − capital expenditures) ÷ revenue, over the
        trailing twelve months
      window: trailing twelve months
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
        - revenue
      prices: []
      entries:
        - fcf_ttm
    explanation:
      plain: |
        Out of every hundred dollars of sales in the last year, how many ended
        up as spendable cash.
      misfires: |
        A single quarter's working capital timing can swing this. Same
        capex-versus-acquisitions confusion as free cash flow itself.
      attribution: |
        Standard. The current-period counterpart to
        [fcf_margin_median_5y].
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          The revenue underneath is the second break rather than a saving one:
          a lender's top line is interest it charged, which moves with the
          policy rate more than with the business.

  - id: fcf_margin_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Free cash flow margin, 5-year median
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        FCF margin(year) = (cash from operations − capital expenditures)
                           ÷ revenue

        value = median( FCF margin(year) for each of the last five fiscal
        years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        Out of every hundred dollars of sales, how many end up as spendable
        cash after keeping the lights on and the equipment current. Ten dollars
        is healthy. Zero means the business grows by consuming everything it
        earns.
      misfires: |
        Companies deliberately in a heavy investment cycle show negative free
        cash flow as a choice, not a failure, and this measure will read low for
        them for years. Also breaks where capex is really acquisitions in
        disguise.
      attribution: |
        Buffett's owner-earnings thinking applied to the income statement.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          Taking the middle of five years does not help — a lender growing
          steadily reports steadily suppressed cash, and one shrinking
          steadily reports steadily enormous cash.

  - id: fcf_yield_on_ev
    kind: computed
    estimator:
      kind: instant
    label: Free cash flow yield on enterprise value
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        (cash from operations − capital expenditures) ÷ enterprise value
      window: trailing twelve months over the current enterprise value
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
      prices:
        - current price
      entries:
        - fcf_ttm
        - enterprise_value
    explanation:
      plain: |
        The cash the whole business throws off each year, divided by what the
        whole business costs including its debts. It is the cash cross-check on
        an earnings-based price: EBIT can be flattered by accounting choices,
        free cash flow is harder to fake.
      misfires: |
        Inherits the lumpy-capex problem of free cash flow. A year of unusually
        light capital spending makes the yield look structurally high when it
        is only temporarily high.
      attribution: |
        Standard value-investing construction; the cash analogue of
        [ev_to_ebit].
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          The price underneath is wrong in the same direction, because it adds
          on borrowing that excludes deposits and then subtracts cash the
          lender is required to hold. A high yield here is the arithmetic, not
          a bargain.
      - data: enterprise value is zero or negative
        because: |
          A negative denominator inverts the yield into nonsense.

  - id: cash_conversion_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Cash conversion, 5-year median
    unit: ratio
    format: "0.00"
    polarity: higher_is_better
    derivation:
      formula: |
        cash conversion(year) = free cash flow ÷ net income

        value = median( cash conversion(year) for each of the last five fiscal
        years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
        - net income
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the reported profits turn into actual money. If a company
        reports a billion in earnings and collects nine hundred million in cash,
        fine. If it reports a billion and collects three hundred million, the
        earnings are an accounting opinion. Persistently below 0.80 means
        accruals, aggressive capitalisation, or receivables that aren't being
        collected.
      misfires: |
        Fast growers show poor conversion because working capital absorbs cash;
        that's normal and not a defect. Software companies with large deferred
        revenue balances show conversion well above 1.0 and look better than
        they are. Companies with high depreciation and amortisation and low
        current capex flatter this badly. A profit small enough to be a
        rounding error against revenue makes a year's ratio enormous without
        saying anything about conversion; the number is right and the reading
        is not.
      attribution: |
        Buffett's owner-earnings thinking, formalised. Also standard in forensic
        accounting (Schilit).
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          Dividing that by an ordinary-sized profit says the business turns
          far more of its earnings into cash than any company could.
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          This divides cash by profit, so a suppressed profit underneath makes
          the conversion read spectacularly well for a reason that is the
          depreciation schedule and nothing else.
      - data: net income in any year of the window is zero or negative
        because: |
          This asks what share of the reported profit arrived as cash, and a
          loss is not a share of anything. Worse than undefined, it inverts:
          a company that lost money AND burned cash divides one negative by
          another and reads as though it converted its earnings perfectly,
          which is the best possible answer to the worst possible facts. A
          loss-making year is real information and belongs on the earnings
          record, where this bank keeps it; it does not belong in a
          conversion ratio.

          Any year and not the median year, because the median is taken of
          the five ratios and one meaningless ratio is enough to be the
          middle one. Dropping the year instead would quietly turn a
          five-point estimator into a four-point one, and a measure that
          becomes a weaker reading when the data thins is the same failure
          wearing a different coat.

  - id: accruals_ratio
    kind: computed
    estimator:
      kind: trailing
    label: Accruals ratio
    unit: ratio
    format: "0.00"
    polarity: lower_is_better
    derivation:
      formula: |
        (net income − cash from operations) ÷ total assets
      window: trailing twelve months over the most recent balance sheet
    inputs:
      filings:
        - net income
        - net cash provided by operating activities
        - total assets
      prices: []
      entries: []
    explanation:
      plain: |
        How much of the reported profit is bookkeeping rather than cash. High
        accruals predict poor subsequent returns with unusual reliability.
      misfires: |
        Rapid growth generates accruals legitimately. Seasonal businesses show
        distorted readings at particular quarter-ends.
      attribution: |
        Sloan (1996). Not Graham, but a check that reported numbers are real,
        which is doing Graham's job with better tools.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Both halves break. The profit-minus-cash figure on top is meant to
          catch earnings that are bookkeeping rather than money, and for a
          lender the cash side of it is the change in the loan book, so the
          subtraction is measuring lending rather than accruals. Underneath,
          the assets are the loan book itself — enormous next to any year's
          earnings — so whatever the top says is divided down to near zero.
          The result is a confident figure close to nothing, every time, which
          reads as a clean bill of health on a measure where low is good.

  - id: effective_tax_rate_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Effective tax rate, 5-year median
    unit: percent
    format: "0.0%"
    polarity: none
    polarity_note: |
      Deliberately none. This is a sanity check, and both extremes are
      informative — an unusually low rate and an unusually high one each say
      something. Neither direction is favourable in itself.
    derivation:
      formula: |
        effective tax rate(year) = income tax expense ÷ pre-tax income

        value = median( effective tax rate(year) for each of the last five
        fiscal years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - income tax expense
        - pre-tax income
      prices: []
      entries: []
    explanation:
      plain: |
        A sanity check on earnings quality. A company reporting a 4% tax rate is
        either running an aggressive structure that will eventually be
        challenged, or booking one-time benefits that inflate earnings per share
        and won't repeat.
      misfires: |
        Companies with large foreign-derived intangible income legitimately run
        low. Loss carryforwards suppress the rate for years after a bad patch.
        REITs pay essentially no corporate tax by design.
      attribution: |
        Not Buffett. Forensic accounting practice. He'd recognise the concern.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          A real estate investment trust pays essentially no corporate tax, by
          statute, in exchange for paying out nearly all of its income. A rate
          near zero here is the form of the company working exactly as
          intended — it is not a tax position anybody chose, and it says
          nothing about whether the earnings are real, which is what this
          measure is for.

  - id: payout_to_fcf_median_5y
    kind: computed
    estimator:
      kind: median
      observations: 5
    label: Total payout over free cash flow, 5-year median
    unit: percent
    format: "0.0%"
    polarity: none
    polarity_note: |
      Deliberately none. A low payout can mean a company is reinvesting or that
      it is hoarding; a high payout can mean generosity or borrowing to pay
      shareholders. The measure does not distinguish these.
    derivation:
      formula: |
        payout(year) = (dividends paid + share repurchases − share issuance)
                       ÷ free cash flow

        value = median( payout(year) for each of the last five fiscal years )
      window: five fiscal years, annual observations, median
    inputs:
      filings:
        - payments of dividends
        - payments for repurchase of common stock
        - proceeds from issuance of common stock
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company is returning more cash than it produces.
        Sustainably above 100% means the returns are being funded with debt.
      misfires: |
        A single large buyback year, or an equity raise inside the window,
        swings the ratio without describing a policy.
      attribution: |
        Standard capital-allocation check, in Buffett's owner-earnings
        tradition.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          Dividing dividends and buybacks by that inflated figure says the
          payout is comfortably covered whatever it is.
      - industry: [real-estate]
        because: |
          A real estate investment trust must pay out nearly all of its
          taxable income to keep its tax status, so a high payout is the form
          of the company rather than a warning about it — and the cash
          underneath is struck after building spending this program cannot
          separate from acquisitions.
      - data: free cash flow in any year of the window is zero or negative
        because: |
          This asks what share of the cash the business produced was handed
          back. Where it produced none, there is no share to state — and the
          ratio does not merely fail to answer, it answers backwards. Divide
          a payout by negative free cash flow and the result is negative, so
          a company borrowing to fund its dividend reads as paying out far
          less than it earns. That is exactly the company this measure exists
          to find, reported as the safest kind there is.

          It was a caution for as long as this measure has existed, saying in
          as many words that those years are uninterpretable — and the median
          was taken anyway. A caution qualifies what a reader sees; the
          arithmetic that reaches a verdict never reads it.

  - id: operating_income_ttm
    kind: computed
    estimator:
      kind: trailing
    label: Operating income, trailing twelve months
    unit: usd
    format: "$0,0"
    polarity: higher_is_better
    derivation:
      formula: |
        OperatingIncomeLoss (TTM)
      window: trailing twelve months
    inputs:
      filings:
        - operating income (loss)
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company makes money from what it actually does, before
        interest and tax. Positive means the operations pay for themselves.
      misfires: |
        A single large one-off charge — an impairment, a legal settlement, a
        restructuring — can push a structurally profitable company negative for
        one period.

        A lender or an insurer that strikes this subtotal at all may strike it
        after the interest it pays to fund itself, or after investment returns
        on money held against claims not yet made — and which of those a filer
        does is its own presentation rather than anything its industry code
        says. Most publish no such subtotal, and this reads absent for them.
        Where one does, read it knowing that "before interest and tax" may not
        mean here what it means elsewhere.
      attribution: |
        Standard. Used as a precondition for any measure that puts earnings in
        a denominator.

  # ===========================================================================
  # Price and valuation
  # ===========================================================================

  - id: market_cap
    kind: computed
    estimator:
      kind: instant
    label: Market capitalization
    unit: usd
    format: "$0,0"
    polarity: none
    polarity_note: |
      Size has no favourable direction. Small companies are where discounts
      concentrate; large companies file more reliably and trade more liquidly.
    derivation:
      formula: |
        shares outstanding × the price of the common stock
      window: current
    inputs:
      filings:
        - common shares outstanding
      prices:
        - the common stock's current price
      entries: []
    explanation:
      plain: |
        What the stock market says the whole company's equity is worth today:
        every share multiplied by the price of one share.

        The price is the company's common stock and only that. Many companies
        have more than one security trading under their name — warrants, units,
        listed bonds, preferred series — and those are worth different amounts
        per unit, so which one is used decides the answer. Which security is
        the common stock is read from the company's own filing cover, where it
        lists what it has registered.
      misfires: |
        Uses shares outstanding at the last filing date, so it lags any
        buyback or issuance since.

        Where a company has several classes of common stock, each is valued at
        its own price and a class that does not trade is valued at the largest
        class that does — the caution beside the figure says how much of the
        company that assumption covered.

        Where the filing states one share count for the whole company and its
        cover does not say which security is the common stock, there is no
        figure: valuing the count at the wrong security would give a confident
        number that is wrong by whatever the two securities differ by, and
        every ratio built on it would be wrong the same way.
      attribution: |
        Standard. Used both as a size test and as an input to several ratios
        below.

  - id: revenue_ttm
    kind: computed
    estimator:
      kind: trailing
    label: Revenue, trailing twelve months
    unit: usd
    format: "$0,0"
    polarity: none
    polarity_note: |
      Size has no favourable direction. What a large company buys you is
      filing quality, analyst attention you may not want, and the ability to
      sell when you choose.
    derivation:
      formula: |
        revenue over the last four quarters
      window: trailing twelve months
    inputs:
      filings:
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        What the company sold in the last year, in money.

        It is here as a measure of how big a company is that does not depend
        on what the market thinks it is worth. Market capitalization is the
        other one, and the difference is not academic: a rule set that hunts
        for companies the market has priced too cheaply, and then requires a
        large market capitalization, is partly rejecting companies for being
        cheap. Sales do not move when the share price does.
      misfires: |
        Sales are not comparable across industries as a measure of size. A
        grocery chain turning over ten billion dollars at two percent margins
        is a smaller business, on any other reading, than a software company
        turning over two billion at thirty. A floor set on revenue is a floor
        on filing quality and tradeability rather than on how substantial the
        company is.

        Companies that shifted between gross and net revenue recognition under
        ASC 606 show a step change across the 2018 boundary that is
        presentation rather than trade.
      attribution: |
        Standard. Graham's own adequate-size test was stated in annual sales —
        $100 million for an industrial company in the 1973 edition — rather
        than in market value.

  - id: enterprise_value
    kind: computed
    estimator:
      kind: instant
    label: Enterprise value
    unit: usd
    format: "$0,0"
    polarity: none
    derivation:
      formula: |
        market cap + total debt − cash & equivalents
      window: current price over the most recent balance sheet
    inputs:
      filings:
        - short-term debt
        - long-term debt
        - cash and cash equivalents
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        What the entire business costs, including its debts. When you buy a
        leveraged company you assume its debts, and the price tag should say so.
      misfires: |
        Mixes a live price against a balance sheet up to a quarter old. Minority
        interests and preferred stock belong in enterprise value and are
        inconsistently tagged.
      attribution: |
        Standard. Defined here because three measures below are built on it.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          This is meant to be what it would cost to buy the whole company: its
          price, plus the debts you take on, less the cash you get. For a
          lender the debts are mostly deposits, which are not counted here,
          and the cash is reserves it is required to hold, which are
          subtracted as though they came free. The figure is confidently too
          small, and every multiple built on it is confidently too cheap.

  - id: pe_ttm
    kind: computed
    estimator:
      kind: instant
    label: Price to earnings, trailing twelve months
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        market capitalization ÷ shares outstanding ÷ diluted earnings per
        share (TTM)
      window: trailing twelve months
    inputs:
      filings:
        - diluted earnings per share
        - common shares outstanding
      prices:
        - the common stock's current price
      entries:
        - market_cap
    explanation:
      plain: |
        What you're paying for a dollar of the last year's earnings. At 20x you
        are paying twenty dollars for each dollar the company earned last year.

        The price per share comes from market capitalization divided by the
        share count rather than straight off a quote. For almost every company
        that is the quoted price to the cent. For a company with two classes of
        common stock it is what the market pays for an average share, which is
        the right partner for an earnings figure that is spread over every
        share there is.
      misfires: |
        A single year's earnings can be unrepresentative in either direction —
        a one-off charge makes the stock look expensive, a one-off gain makes it
        look cheap. Cyclicals look cheapest at the top of the cycle.

        Needs a share count as well as a price, so it is unavailable wherever
        market capitalization is.
      attribution: |
        Standard. The trailing counterpart to [pe_3y_avg_eps].
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          The earnings underneath are after that charge, so a property company
          reads as expensive on this for a reason that is the accounting
          rather than the price.
      - data: diluted earnings per share (TTM) is zero or negative
        because: |
          A negative denominator makes the ratio meaningless rather than
          negative.

  - id: pe_3y_avg_eps
    kind: computed
    estimator:
      kind: instant
    label: Price to earnings on three-year average EPS
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        market capitalization ÷ shares outstanding ÷ mean(diluted EPS, last 3
        fiscal years)
      window: three fiscal years, annual observations, mean
    inputs:
      filings:
        - diluted earnings per share
        - common shares outstanding
      prices:
        - the common stock's current price
      entries:
        - market_cap
    explanation:
      plain: |
        What you're paying for a dollar of typical earnings. Averaging three
        years is the point: it stops you paying a low multiple on a fluke good
        year.

        The price per share is read the same way as for the trailing measure —
        market capitalization over the share count, so both are paying for the
        same thing. See [pe_ttm].
      misfires: |
        Cyclicals at the trough have collapsed earnings, so this explodes and
        they look expensive at the exact moment they're cheapest. Any large
        one-off charge inside the three-year window drags the average down and
        makes the stock look dearer than it is.

        Needs a share count as well as a price, so it is unavailable wherever
        market capitalization is.
      attribution: |
        Graham, *The Intelligent Investor*, chapter 14. The three-year averaging
        is his method.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          Averaging three years of those earnings smooths them without fixing
          them.
      - data: the three-year average diluted EPS is zero or negative
        because: |
          Negative average EPS makes the ratio meaningless.

  - id: price_to_book
    kind: computed
    estimator:
      kind: instant
    label: Price to book
    unit: times
    format: "0.00x"
    polarity: lower_is_better
    derivation:
      formula: |
        market capitalization ÷ total shareholders' equity
      window: current price over the most recent balance sheet
    inputs:
      filings:
        - total shareholders' equity
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        What you pay for a dollar of the company's accounting net worth — the
        assets it reports, less what it owes.
      misfires: |
        This one is severe. Book value stopped describing modern businesses
        sometime around 1990: software, pharma, brands, and services companies
        carry their real assets off the balance sheet entirely, so this measure
        systematically rejects them. Where it still works well is banks,
        insurers, and asset-heavy industrials.
      attribution: |
        Graham.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          Book value for a property company is buildings at what they cost,
          less every year of depreciation charged since — which for well-kept
          property is a long way below what they are worth. The company also
          pays out nearly all its income by law, so the accumulated-profit
          line is negative rather than small. A property company can read as
          expensive here while trading below the value of what it owns.
      - data: total shareholders' equity is zero or negative
        because: |
          Companies with buyback-driven negative equity produce a meaningless
          ratio.

  - id: price_to_net_tangible_assets
    kind: computed
    estimator:
      kind: instant
    label: Price to net tangible assets
    unit: times
    format: "0.00x"
    polarity: lower_is_better
    derivation:
      formula: |
        market capitalization
        ÷ (total equity − goodwill − intangible assets)
      window: current price over the most recent balance sheet
    inputs:
      filings:
        - total shareholders' equity
        - goodwill
        - intangible assets excluding goodwill
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        Price to book with the imaginary parts removed. What you're paying per
        dollar of things that could actually be sold.
      misfires: |
        Asset-light businesses fail this by construction — there is nothing
        tangible to price.
      attribution: |
        Graham. The stricter form of price to book.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The same understated book value as the plain version, and stripping
          goodwill does nothing to fix it: what is understated here is the
          buildings, which are as tangible as an asset gets.
      - data: net tangible assets (equity − goodwill − intangibles) is zero or
          negative
        because: |
          Companies with negative tangible book, common after large
          acquisitions, produce a meaningless ratio.

  - id: graham_combined_multiple
    kind: computed
    estimator:
      kind: instant
    label: Graham combined multiple
    unit: ratio
    format: "0.0"
    polarity: lower_is_better
    derivation:
      formula: |
        P/E (3-yr average EPS) × price to book
      window: current price over three-year average EPS and the most recent
        balance sheet
    inputs:
      filings: []
      prices: []
      entries:
        - pe_3y_avg_eps
        - price_to_book
    explanation:
      plain: |
        A single test that lets one number be high if the other is low. A stock
        at 18x earnings and 1.2x book scores 21.6; one at 18x and 2.0x scores
        36.
      misfires: |
        Inherits every problem of both components, and compounds them: a
        cyclical at trough earnings with an asset-light model fails twice for
        two unrelated bad reasons.
      attribution: |
        Graham. Often called the Graham Number in his honour. The product form
        is exactly how he derived it.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          Both factors are already wrong for a property company — earnings
          suppressed by building depreciation, book value held at what the
          buildings cost decades ago — and multiplying them together compounds
          it rather than cancelling it.
      - data: either component is not meaningful
        because: |
          The product of a meaningless factor is meaningless.

  - id: owner_earnings_yield
    kind: computed
    estimator:
      kind: instant
    label: Owner earnings yield
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        (cash flow from operations − maintenance capex) ÷ market capitalization

        where maintenance capex is proxied as min(capex, depreciation &
        amortization)
      window: trailing twelve months over the current market capitalization
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
        - depreciation, depletion and amortization
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        The cash the business throws off for its owners each year, divided by
        what you'd pay for the whole thing. At 5% you're buying a dollar of
        annual owner cash for twenty dollars. It's the closest thing available
        to "what am I actually getting for my money."
      misfires: |
        Cyclicals at the top of the cycle look cheapest exactly when they're
        most dangerous, because earnings are at peak. Any company with genuinely
        lumpy capex will have years where min(capex, D&A) badly misstates
        maintenance. Companies whose capex is really disguised M&A, or the
        reverse, break the proxy entirely.
      attribution: |
        Buffett, from the 1986 shareholder letter appendix. He would recognise
        owner earnings; he'd quibble that the maintenance capex proxy is crude,
        and he'd be right.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          Owner earnings start from that figure, so what this reports as the
          cash a lender throws off for its owners is the loan book moving. A
          growing lender reads poor and a shrinking one reads superb, which is
          the wrong way round on the measure a purchase can turn on.
      - industry: [real-estate]
        because: |
          Owner earnings take cash from operations and subtract the smaller of
          capital spending and depreciation, as a stand-in for what it costs
          to keep the assets going. For a property company that subtraction is
          wrong twice: the depreciation is a convention rather than a cost,
          and what it actually spends on buildings sits under captions this
          program deliberately refuses to guess at.

  - id: owner_earnings_yield_on_ev
    kind: computed
    estimator:
      kind: instant
    label: Owner earnings yield on enterprise value
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        (cash flow from operations − maintenance capex) ÷ enterprise value

        where maintenance capex is proxied as min(capex, depreciation and
        amortization LESS the amortization of intangible assets)
      window: trailing twelve months over the current enterprise value
    inputs:
      filings:
        - net cash provided by operating activities
        - payments to acquire property, plant and equipment
        - depreciation, depletion and amortization
        - amortization of intangible assets
      prices:
        - current price
      entries:
        - enterprise_value
    explanation:
      plain: |
        The cash the business throws off for its owners each year, divided by
        what the whole business costs — the shares AND the debts that come
        with them.

        It differs from the plainer owner earnings yield in two ways, and both
        of them make the answer smaller for the companies that were being
        flattered.

        The price is the enterprise value. Owner earnings is a figure about
        the business, and buying a business with borrowings attached means
        taking on the borrowings; priced against the shares alone, a heavily
        indebted company looks cheap precisely for being indebted.

        And the maintenance-capex proxy leaves out the amortization of
        intangibles. The proxy works on the reasoning that a business
        replacing its equipment spends about what it writes it down by. A
        customer list bought in an acquisition is written down over ten years
        and is never replaced by spending — it is replaced by another
        acquisition. Counting it raises the floor of the proxy, so it
        subtracts less, so owner earnings come out higher — and they come out
        highest for the serial acquirers where the reasoning holds least.
      misfires: |
        It inherits every problem of the plainer version: cyclicals look
        cheapest at the peak, lumpy capital spending makes the proxy wander,
        and capital spending that is really acquisition in disguise breaks it
        outright.

        It adds one of its own. A filer who never reports the amortization of
        intangibles separately from depreciation has no figure here at all,
        so this reads absent where the plainer version reads a number. That is
        deliberate — filling the gap with total D&A would report the
        overstated answer under the corrected name.
      attribution: |
        Owner earnings is Buffett's, from the 1986 shareholder letter
        appendix. Neither refinement is his: pricing a business-level figure
        against enterprise value is standard practice, and taking acquired
        intangible amortization out of the proxy answers a specific objection
        about serial acquirers rather than anything he wrote.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Free cash flow starts from cash from operations, and for a lender
          that figure moves with the period's change in loans and deposits
          rather than with the business. A bank that is shrinking its loan
          book generates the most of it — so this does not come back small for
          a lender in trouble, it comes back large. An insurer's is inflated
          the same way by float: premiums arrive before claims are paid, which
          looks like cash the business made and is really cash it is holding
          for somebody else.

          And the price underneath leaves out the deposits and reserves that
          are most of what these companies owe. Both halves point the same
          way, so the yield comes out several times what any real reading
          would be.
      - industry: [real-estate]
        because: |
          The same two faults as the version measured against market value,
          plus a price that treats a property company's mortgages as though
          they were an ordinary company's borrowing.
      - data: enterprise value is zero or negative
        because: |
          A company holding more cash than its shares and its debts are worth
          together prices at nothing or less, and a yield measured against
          that is not a yield. Divide by a negative and it inverts: the more
          the business earns, the worse the number reads, on a measure where
          higher is better.

          The formula has always refused on this and cited "the bank's own
          test" for it. Its sibling measured against market value states the
          condition; this one did not, so a reader following the sentence
          here found nothing to check it against.

  - id: ev_to_ebit
    kind: computed
    estimator:
      kind: instant
    label: Enterprise value to EBIT
    unit: times
    format: "0.0x"
    polarity: lower_is_better
    derivation:
      formula: |
        (market cap + total debt − cash & equivalents) ÷ EBIT (TTM)
      window: current enterprise value over trailing twelve months EBIT
    inputs:
      filings:
        - EBIT
      prices:
        - current price
      entries:
        - enterprise_value
    explanation:
      plain: |
        What the entire business costs, including its debts, per dollar of
        operating profit. Enterprise value rather than market cap because when
        you buy a leveraged company you assume its debts, and the price tag
        should say so. Inverted, 8x is a 12.5% pre-tax operating earnings yield.
      misfires: |
        A single depressed or inflated year of EBIT moves this a long way.
        Companies whose operating income is dominated by one segment in trouble
        read cheap on a consolidated basis.
      attribution: |
        Greenblatt's earnings yield, inverted. With a caveat that matters: his
        method is a cross-sectional ranking — buy the cheapest 30 out of 3,500,
        whatever the absolute number happens to be that year — so he would not
        endorse any fixed level of this measure.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          The price on top is already the wrong quantity for these companies:
          enterprise value adds on the debts a buyer would take on, and for a
          lender those are deposits and for an insurer they are claims not yet
          made — neither of which any filer tags as borrowing. The multiple
          therefore reads as though the whole company could be had for its
          market value plus a rounding error, whatever it earns.
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          The profit underneath is after that charge, so a property company
          reads as expensive on this whatever it is worth.
      - data: EBIT (TTM) is zero or negative
        because: |
          A multiple says how many years of profit the price represents, and
          a company that made no profit has none to count. Negative is worse
          than undefined: dividing a positive price by a loss gives a
          negative multiple, which sorts alongside the cheapest companies on
          a measure where lower is better — so the worst case reads as the
          best bargain.

          The formula has always refused on this and cited "the bank's own
          test" for it. The test was not here; it is now.

  - id: ev_ebit_to_own_5y_median
    kind: computed
    estimator:
      kind: instant
    label: EV/EBIT versus its own five-year median
    unit: times_own_median
    format: "0.00x"
    polarity: lower_is_better
    derivation:
      formula: |
        EV/EBIT (current) ÷ median( EV/EBIT over 20 trailing quarters )
      window: current against a five-year quarterly median
    inputs:
      filings:
        - EBIT
        - short-term debt
        - long-term debt
        - cash and cash equivalents
      prices:
        - current price
        - historical quarterly prices, 20 quarters
      entries:
        - ev_to_ebit
    explanation:
      plain: |
        How the business is priced today compared with how the market has
        usually priced this particular business. At 0.70 the market has put this
        company at 30% below its own normal. Absolute multiples mean different
        things for different businesses; this asks a company-specific question
        instead.
      misfires: |
        A company whose business has permanently deteriorated has a median that
        describes a company that no longer exists, and it will read as a
        discount all the way down.
      attribution: |
        Graham's discount-closure logic, measured against the company's own
        history rather than a universal standard.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          This compares today's multiple against that same company's own
          recent history, and the multiple it compares is one that does not
          describe these companies at any date. A discount against a
          meaningless median is meaningless twice over.
      - data: fewer than 20 trailing quarters of EV/EBIT are available
        because: |
          Five years of data is the minimum; anything shorter and the median
          is noise.
      - data: EV/EBIT is not meaningful in the current period
        because: |
          A ratio against a median needs a current value to compare.

  - id: pe_to_own_5y_median_pe
    kind: computed
    estimator:
      kind: instant
    label: P/E versus its own five-year median P/E
    unit: times_own_median
    format: "0.00x"
    polarity: lower_is_better
    derivation:
      formula: |
        current P/E (TTM) ÷ median( quarterly P/E over 5 yrs )
      window: current against a five-year quarterly median
    inputs:
      filings:
        - diluted earnings per share
      prices:
        - current price
        - historical quarterly prices, 20 quarters
      entries:
        - pe_ttm
    explanation:
      plain: |
        Whether the stock is cheap compared to how the market has usually priced
        this particular company. Absolute multiples mean different things for
        different businesses; this asks a company-specific question instead.
      misfires: |
        A company whose business has genuinely changed — a hardware company that
        became a software company — has a stale median that describes a company
        that no longer exists. Five years of persistent multiple compression
        makes a falling knife look cheap.
      attribution: |
        Lynch, applied to a company's own history.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          This compares today's price against what the same company usually
          trades at, and the earnings both readings rest on are suppressed by
          building depreciation at every date. A discount against a distorted
          median is not a discount.
      - data: fewer than 20 trailing quarters of P/E are available
        because: |
          A five-year median needs five years.
      - data: P/E (TTM) is not meaningful in the current period
        because: |
          A ratio against a median needs a current value to compare.

  - id: peg_trailing
    kind: computed
    estimator:
      kind: instant
    label: PEG ratio, trailing
    unit: ratio
    format: "0.00"
    polarity: lower_is_better
    derivation:
      formula: |
        P/E (TTM) ÷ (5-yr diluted EPS CAGR expressed as a whole number)

        A company at 24x earnings growing 20% a year has a PEG of 1.2.
      window: current P/E over a five-year annual growth rate
    inputs:
      filings:
        - diluted earnings per share
      prices:
        - current price
      entries:
        - pe_ttm
        - eps_cagr_5y
    explanation:
      plain: |
        Whether the price you're paying is justified by the speed the company is
        growing. Lynch's formulation: the P/E of any fairly priced company will
        equal its growth rate.
      misfires: |
        This version uses trailing growth, and Lynch's own PEG used projected
        growth. That inverts the measure's behaviour at the moment it matters
        most: it will look good on companies whose growth has just peaked and is
        about to roll over, which is exactly the situation forward estimates
        were used to avoid. Slow growers with 2% growth produce absurd values.
        Cyclicals coming off a trough show enormous trailing growth and a
        flattering reading at exactly the wrong moment.
      attribution: |
        Lynch, with the forward-looking growth input substituted for a trailing
        one.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          Both halves rest on earnings that building depreciation suppresses:
          the price against them on top, and their growth rate underneath. The
          industry uses funds from operations for exactly this reason, and
          this program does not compute it.
      - data: the five-year EPS CAGR is not meaningful
        because: |
          A near-zero or negative base year makes the growth rate meaningless,
          and the ratio built on it with it.
      - data: P/E (TTM) is not meaningful
        because: |
          A negative or absent numerator has no interpretation here.

  - id: dividend_adjusted_peg
    kind: computed
    estimator:
      kind: instant
    label: Dividend-adjusted PEG
    unit: ratio
    format: "0.00"
    polarity: lower_is_better
    derivation:
      formula: |
        P/E ÷ (5-yr EPS CAGR + dividend yield)
      window: current P/E over a five-year annual growth rate plus current yield
    inputs:
      filings:
        - diluted earnings per share
        - payments of dividends
      prices:
        - current price
      entries:
        - pe_ttm
        - eps_cagr_5y
        - dividend_yield
    explanation:
      plain: |
        The same question as PEG, but counting the dividend as part of what the
        company gives you. A company growing 6% and paying a 4% yield is
        returning 10% a year in total, and this measure prices it that way.
      misfires: |
        Inherits every problem of the trailing PEG. Additionally, a dividend
        that is about to be cut is counted at full value here.
      attribution: |
        Lynch, used explicitly for stalwarts and slow growers, where the plain
        PEG is useless.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The price-to-earnings half is suppressed by building depreciation,
          and the dividend added underneath is one a property company pays
          because the law requires it rather than because the business is
          generous. Both faults push the same way, which makes every REIT look
          like a bargain.
      - data: the five-year EPS CAGR is not meaningful
        because: |
          The denominator inherits it.
      - data: P/E (TTM) is not meaningful
        because: |
          A negative or absent numerator has no interpretation here.

  - id: dividend_yield
    kind: computed
    estimator:
      kind: instant
    label: Dividend yield
    unit: percent
    format: "0.00%"
    polarity: none
    polarity_note: |
      A high yield can mean generous distribution or a collapsing price. No
      intrinsically favourable direction.
    derivation:
      formula: |
        dividends paid (TTM) ÷ market capitalization
      window: trailing twelve months over current market capitalization
    inputs:
      filings:
        - payments of dividends
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        The cash the company paid out to shareholders over the last year, as a
        percentage of what the whole company costs today.
      misfires: |
        A special one-off dividend inside the window inflates the yield. A
        recently initiated or recently cut dividend is misstated by a trailing
        window in both directions.
      attribution: |
        Standard. Defined here as an input to [dividend_adjusted_peg].

  - id: ncav_to_market_cap
    kind: computed
    estimator:
      kind: instant
    label: Net current asset value over market capitalization
    unit: ratio
    format: "0.00"
    polarity: higher_is_better
    derivation:
      formula: |
        (current assets − total liabilities) ÷ market capitalization
      window: most recent balance sheet over current market capitalization
    inputs:
      filings:
        - total current assets
        - total liabilities
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        How much of the purchase price is covered by liquid assets net of all
        debts. At 1.5 you'd be buying the company for two-thirds of its net
        current assets and getting the business itself free. That's Graham's
        net-net, and it barely exists in modern markets outside of Japan and
        micro-caps.
      misfires: |
        Current assets are carried at book, and inventory in particular rarely
        realises book in a wind-down. The measure assumes an orderly liquidation
        that would not actually happen.
      attribution: |
        Graham. The net-net construction.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          Banks, insurers and property companies do not divide the balance
          sheet into what falls due within the year and what does not, so
          there is no working capital to read and no current ratio to strike.
          This is not a filing that is missing something: the division does
          not exist for these businesses, and it never will.

          Net current asset value is current assets less every liability, so
          without that division there is nothing to compute it from.

  - id: net_cash_to_market_cap
    kind: computed
    estimator:
      kind: instant
    label: Net cash over market capitalization
    unit: ratio
    format: "0.00"
    polarity: higher_is_better
    derivation:
      formula: |
        (cash + short-term investments − total debt) ÷ market capitalization
      window: most recent balance sheet over current market capitalization
    inputs:
      filings:
        - cash and cash equivalents
        - short-term investments
        - short-term debt
        - long-term debt
      prices:
        - current price
      entries:
        - market_cap
    explanation:
      plain: |
        How much of the price you're paying is already sitting in the bank. It
        lowers the effective multiple on the operating business and it's a
        cushion during a stumble.
      misfires: |
        Cash held overseas, pledged, or committed to an announced acquisition is
        counted here as if it were free. Companies with captive finance arms
        show debt that funds a loan book.
      attribution: |
        Lynch. He liked knowing how much of the price was already money.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          Net cash is what is left after paying off the debts, and for these
          companies the figure it subtracts is not the debts. Deposits and
          claims not yet made are missing from it, while the cash on top is
          reserves a lender is required to hold. A bank can read as though a
          large part of its market value were spare cash sitting in the
          building.

  # ===========================================================================
  # Growth
  # ===========================================================================

  - id: incremental_roic_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Incremental return on invested capital, 5-year
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        NOPAT(year)    = EBIT × (1 − effective tax rate)
        invested(year) = total debt + total equity − cash & equivalents

        base_profit  = mean( NOPAT, the 3 fiscal years ending 5 years before
                             the most recent 3 )
        end_profit   = mean( NOPAT, the most recent 3 fiscal years )
        base_capital = mean( invested, the same 3 early years )
        end_capital  = mean( invested, the most recent 3 years )

        value = (end_profit − base_profit) ÷ (end_capital − base_capital)
      window: eight fiscal years, three-year means at each end, five years
        centre to centre
    inputs:
      filings:
        - EBIT
        - income tax expense
        - pre-tax income
        - short-term debt
        - long-term debt
        - total shareholders' equity
        - cash and cash equivalents
      prices: []
      entries: []
    explanation:
      plain: |
        What the money the company kept has earned — as opposed to what the
        money already in the business earns.

        Return on invested capital describes the existing base: everything
        built, bought and borrowed over the company's whole life, taken
        together. This describes only the increment. Profit is higher than it
        was five years ago by some amount; the capital tied up in the business
        is higher by some amount; the first divided by the second is what the
        retained profits bought.

        It is the number that decides how fast a business compounds. A company
        earning 30% on its existing base and 6% on everything new is a company
        that has run out of places to put money, and the 30% will drift down
        toward the 6% for as long as it keeps reinvesting. The base tells you
        what was built. This tells you what is being built now.
      misfires: |
        It is a ratio of two differences, so it is more sensitive to the
        endpoints than any level measure — which is why both ends are
        three-year means, and why it needs eight fiscal years to say anything
        at all.

        A large acquisition lands its whole price in the denominator at once
        and its profits arrive over years, so an acquisitive company reads low
        for a while after a deal and then recovers. The reverse holds for
        divestitures.

        A company that funds growth off the balance sheet — leases before ASC
        842, or an asset-light model where the capital sits with suppliers —
        shows little change in invested capital, so the ratio reads very high
        on a small denominator.
      attribution: |
        Buffett's, in substance rather than in this arithmetic. His stated
        test for retained earnings is whether each dollar kept has created at
        least a dollar of market value, which is the same question asked of
        the market rather than of the filings.
    not_meaningful_when:
      - industry: [depository-lending, insurance, real-estate]
        because: |
          This asks what the profit added over five years was worth against
          the capital added to produce it, and for these companies both
          figures are the wrong quantity — the same faults that make return on
          invested capital unreadable here, measured as a change instead of a
          level.
      - data: invested capital did not grow across the window
        because: |
          No capital was retained for this to be the return on. A company that
          grew while keeping nothing is a real and interesting case, and it is
          one this ratio has no way to express: dividing by zero or by a
          negative would print a figure that reads like an answer.
      - data: pre-tax income in any year of the window is zero or negative
        because: |
          The effective tax rate for that year has no meaning, so after-tax
          operating profit cannot be built for the window.
      - data: fewer than eight fiscal years are available on one accounting
          basis
        because: |
          The window cannot be constructed, and there is deliberately no
          shorter fallback — the same reasoning as the averaged growth rates.

  - id: revenue_cagr_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Revenue CAGR, 5-year
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        base = mean( revenue, the 3 fiscal years ending 5 years before the
                     most recent 3 )
        end  = mean( revenue, the most recent 3 fiscal years )

        value = (end ÷ base)^(1/5) − 1
      window: eight fiscal years, three-year means at each end, five years
        centre to centre
    inputs:
      filings:
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        How fast sales have grown per year over five years. Roughly, whether the
        business is growing at least as fast as the economy around it — nominal
        GDP growth is the natural yardstick.
      misfires: |
        A large divestiture drops revenue permanently without anything being
        wrong. Companies that shifted from gross to net revenue recognition
        under ASC 606 show artificial declines across the 2018 boundary.
        The three-year means need eight fiscal years on one accounting
        basis, which is a real cost: measured across a broad sample of
        filers it is unavailable for about one company in six that a
        single-year version could answer for, split between recent listings
        and restatements that were never carried back through the older
        years.
      attribution: |
        More Lynch than Buffett. Buffett would accept it as sensible but
        wouldn't lead with it; he's held slow growers happily when the returns
        on capital were high enough.
    not_meaningful_when:
      - data: mean revenue over the three base years is zero or negative
        because: |
          The compound growth rate has no interpretation from a zero or
          negative base.
      - data: fewer than eight fiscal years of revenue are available on one
          accounting basis
        because: |
          The window cannot be constructed. There is deliberately no
          shorter-window fallback: a measure that quietly becomes a weaker
          estimator when the data thins reads the same on screen and means
          something else.

  - id: revenue_cagr_3y
    kind: computed
    estimator:
      kind: averaged
      observations: 6
    label: Revenue CAGR, 3-year
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        base = mean( revenue, the 3 fiscal years ending 3 years before the
                     most recent 3 )
        end  = mean( revenue, the most recent 3 fiscal years )

        value = (end ÷ base)^(1/3) − 1
      window: six fiscal years, three-year means at each end, three years
        centre to centre
    inputs:
      filings:
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        How fast sales have grown per year over three years. The shorter window
        is the faster-moving signal: it shows whether the top line has
        decelerated recently, which a five-year window would smooth away. The
        two three-year averages it compares do not overlap, so it is still a
        statement about recent years rather than a slower version of the
        five-year measure.
      misfires: |
        Same divestiture and revenue-recognition distortions as the five-year
        measure, and more exposed to them because there are fewer years to
        dilute a single bad one. Six fiscal years on one accounting basis are
        needed, which is two more than the endpoint version wanted.
      attribution: |
        Lynch. Recency over smoothness.
    not_meaningful_when:
      - data: mean revenue over the three base years is zero or negative
        because: |
          The compound growth rate has no interpretation from a zero or
          negative base.
      - data: fewer than six fiscal years of revenue are available on one
          accounting basis
        because: |
          The window cannot be constructed.

  - id: revenue_change_yoy
    kind: computed
    estimator:
      kind: endpoint
    label: Revenue change, year over year
    unit: percent
    format: "+0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        revenue (TTM) ÷ revenue (prior year TTM) − 1
      window: trailing twelve months against the prior trailing twelve months
    inputs:
      filings:
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company is selling more or less than it was a year ago. This
        is the difference between a business having a bad year and a business
        disappearing: a 5% decline in a cyclical downturn is one thing, a 20%
        decline in a structurally challenged industry is another.
      misfires: |
        Deep cyclicals at a genuine trough post large declines that are the
        setup rather than the problem. Distinguishing a cyclical trough from a
        structural collapse in the numbers alone isn't reliably possible.
        Acquisitions and divestitures move this without organic change.
      attribution: |
        Standard. Used here as a survival check rather than a growth check.

  - id: net_income_cagr_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Net income CAGR, 5-year
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        base = mean( net income, the 3 fiscal years ending 5 years before
                     the most recent 3 )
        end  = mean( net income, the most recent 3 fiscal years )

        value = (end ÷ base)^(1/5) − 1
      window: eight fiscal years, three-year means at each end, five years
        centre to centre
    inputs:
      filings:
        - net income
      prices: []
      entries: []
    explanation:
      plain: |
        How fast the company's bottom-line profit has grown per year over five
        years.
      misfires: |
        The three-year means are what make this usable at all. On single
        years, a one-off charge in the base year is the single most damaging
        thing that can happen to it — not because the answer looks wrong,
        but because it does not: a company growing at 7% whose base year
        carried a charge reads around 20%, which sits comfortably inside any
        plausible band and passes.

        The 2017 tax act permanently changed effective rates and distorts any
        window spanning it. Eight fiscal years on one accounting basis are
        needed, so this is absent for recent listings and for companies whose
        older years were never re-reported after a restatement.
      attribution: |
        Standard. Defined here as an input to
        [ni_minus_revenue_cagr_spread_5y].
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          A growth rate in a figure suppressed by that charge tracks the
          depreciation schedule and the timing of building sales as much as
          the business, and both are lumpy.
      - data: mean net income over the three base years is zero or negative
        because: |
          The compound growth rate has no interpretation from a zero or
          negative base.
      - data: mean net income over the three base years is at most a tenth of
          the mean over the three end years, AND the base years' net margin is
          under half the end years'
        because: |
          Both together say the earnings did not grow, they appeared — a
          margin recovery or a turnaround rather than a business selling more.
          A compound rate then measures the recovery, which has a ceiling that
          selling more does not.

          Neither half is enough alone, and that is measured rather than
          assumed. The margin half on its own is algebraically a
          margin-expansion test — which this bank already publishes as
          [ni_minus_revenue_cagr_spread_5y] — and across a broad sample of
          filers it fires on companies whose base-period earnings were
          substantial. The multiple half on its own fires on a company whose
          revenue multiplied about as fast as its earnings, which is growth.
      - data: fewer than eight fiscal years of net income are available on one
          accounting basis
        because: |
          The window cannot be constructed.

  - id: eps_cagr_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Diluted EPS CAGR, 5-year
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        base = mean( diluted EPS, the 3 fiscal years ending 5 years before
                     the most recent 3 )
        end  = mean( diluted EPS, the most recent 3 fiscal years )

        value = (end ÷ base)^(1/5) − 1
      window: eight fiscal years, three-year means at each end, five years
        centre to centre
    inputs:
      filings:
        - diluted earnings per share
      prices: []
      entries: []
    explanation:
      plain: |
        How fast the profit attributable to each share you own has grown per
        year over five years. Per-share rather than total, so a company that
        grew by issuing stock gets no credit for it.
      misfires: |
        Buyback-heavy companies show EPS growth that isn't operating growth.
        The three-year means at each end are what stop the answer depending
        on which two years happen to sit at the ends, and they cost eight
        fiscal years of history on one accounting basis.
      attribution: |
        Lynch. The growth input to his PEG.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          Earnings per share carry that charge like any other earnings figure,
          and its growth rate carries it compounded.
      - data: mean diluted EPS over the three base years is zero or negative
        because: |
          A zero or negative base mean breaks the compound rate entirely.
      - data: mean net income over the three base years is at most a tenth of
          the mean over the three end years, AND the base years' net margin is
          under half the end years'
        because: |
          Both together say the earnings did not grow, they appeared — a
          margin recovery or a turnaround rather than a business selling more.
          A compound rate then measures the recovery, which has a ceiling that
          selling more does not.

          Neither half is enough alone, and that is measured rather than
          assumed. The margin half on its own is algebraically a
          margin-expansion test — which this bank already publishes as
          [ni_minus_revenue_cagr_spread_5y] — and across a broad sample of
          filers it fires on companies whose base-period earnings were
          substantial. The multiple half on its own fires on a company whose
          revenue multiplied about as fast as its earnings, which is growth.
      - data: fewer than eight fiscal years of diluted EPS are available on
          one accounting basis
        because: |
          The window cannot be constructed.

  - id: eps_growth_10y
    kind: computed
    estimator:
      kind: averaged
      observations: 10
    label: Ten-year EPS growth, three-year averages at both ends
    unit: percent
    format: "0.0%"
    polarity: higher_is_better
    derivation:
      formula: |
        mean(EPS, most recent 3 yrs) ÷ mean(EPS, 3 yrs ending 10 yrs ago) − 1
      window: ten fiscal years, three-year mean at each end
    inputs:
      filings:
        - diluted earnings per share
      prices: []
      entries: []
    explanation:
      plain: |
        How much more the company earns per share than it did a decade ago,
        stated as a total rather than an annual rate. A third more over ten
        years is under 3% a year.
      misfires: |
        Requires ten years of filings, which excludes anything that listed
        recently. The three-year averaging at both ends is what makes this
        usable at all — single-year endpoints make the number meaningless.
      attribution: |
        Graham. Both the ten-year span and the three-year averaging are his
        method.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          Over ten years the distortion accumulates rather than averaging out,
          because the depreciation charged rises with every building bought.
      - data: the earlier three-year mean EPS is zero or negative
        because: |
          The growth ratio has no interpretation from a zero or negative base.
      - data: fewer than ten fiscal years of EPS are available
        because: |
          The window cannot be constructed.
      - data: mean net income over the three base years is at most a tenth of
          the mean over the three end years, AND the base years' net margin is
          under half the end years'
        because: |
          Both together say the earnings did not grow, they appeared — a
          margin recovery or a turnaround rather than a business selling more.
          A compound rate then measures the recovery, which has a ceiling that
          selling more does not.

          Neither half is enough alone, and that is measured rather than
          assumed. The margin half on its own is algebraically a
          margin-expansion test — which this bank already publishes as
          [ni_minus_revenue_cagr_spread_5y] — and across a broad sample of
          filers it fires on companies whose base-period earnings were
          substantial. The multiple half on its own fires on a company whose
          revenue multiplied about as fast as its earnings, which is growth.

  - id: ni_minus_revenue_cagr_spread_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Net income CAGR minus revenue CAGR, 5-year
    unit: percentage_points
    format: "+0.0 pp"
    polarity: none
    polarity_note: |
      Deliberately none. A positive spread means margins are expanding, which
      reads as operating leverage to one investor and as an unsustainable
      one-off to another. Both readings are held by profiles in this tool.
    derivation:
      formula: |
        (5-yr net income CAGR) − (5-yr revenue CAGR), in percentage points
      window: eight fiscal years, three-year means at each end, both series
    inputs:
      filings: []
      prices: []
      entries:
        - net_income_cagr_5y
        - revenue_cagr_5y
    explanation:
      plain: |
        Whether profits are growing at least as fast as sales. If sales grow 8%
        and profits grow 3%, the company is buying its growth by giving away
        margin.
      misfires: |
        The 2017 tax act permanently changed effective rates and distorts any
        window spanning it. Both components now average three fiscal years at
        each end, so a one-time charge in a single year no longer swings this
        — at the cost of needing eight years of both series over the same
        fiscal years.
      attribution: |
        Buffett's margin-discipline thinking, expressed as a spread.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          This reads the distance between profit growth and revenue growth as
          a statement about margins. For a property company the profit leg is
          suppressed by building depreciation and punctuated by gains on
          buildings sold, while the revenue leg is clean — so the spread
          reports the depreciation schedule and calls it a margin trend.
      - data: either component CAGR is not meaningful
        because: |
          The difference of a meaningless number is meaningless.

  - id: eps_minus_revenue_cagr_spread_5y
    kind: computed
    estimator:
      kind: averaged
      observations: 8
    label: Diluted EPS CAGR minus revenue CAGR, 5-year
    unit: percentage_points
    format: "+0.0 pp"
    polarity: none
    polarity_note: |
      Deliberately none. A wide positive spread is genuine operating leverage in
      a scaling business and exhausted margin-and-buyback arithmetic in a mature
      one. The measure cannot tell them apart.
    derivation:
      formula: |
        (5-yr diluted EPS CAGR) − (5-yr revenue CAGR), in percentage points
      window: eight fiscal years, three-year means at each end, both series
    inputs:
      filings: []
      prices: []
      entries:
        - eps_cagr_5y
        - revenue_cagr_5y
    explanation:
      plain: |
        Whether the earnings growth is coming from the business or from
        financial engineering. Margin expansion and buybacks are real and
        legitimate, but a company growing EPS at 30% on 4% revenue growth has
        nearly exhausted both, and there is no third act.
      misfires: |
        A large buyback programme inside the window widens the spread without
        any operating change. Both components average three fiscal years at
        each end, so what it no longer inherits is either one's sensitivity
        to a single year.
      attribution: |
        Lynch. The check on where growth in earnings per share is coming from.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The same fault as the version built on net income: earnings per
          share carry the building depreciation and the gains on buildings
          sold, rent does not, and the distance between them is reported as
          operating leverage.
      - data: either component CAGR is not meaningful
        because: |
          The difference of a meaningless number is meaningless.

  - id: inventory_minus_revenue_growth_yoy
    kind: computed
    estimator:
      kind: endpoint
    label: Inventory growth minus revenue growth, year over year
    unit: percentage_points
    format: "+0.0 pp"
    polarity: lower_is_better
    derivation:
      formula: |
        (inventory YoY % change) − (revenue YoY % change)

        Year-over-year specifically, never sequential.
      window: most recent period against the same period one year earlier
    inputs:
      filings:
        - inventory, net
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        Whether product is piling up in the warehouse faster than it's selling.
        This is one of the oldest and most reliable early warnings that demand
        is softening, and it shows up in the balance sheet a quarter or two
        before it shows up in the income statement.
      misfires: |
        Deliberate builds ahead of a product launch or through a supply chain
        disruption look identical to demand weakness in the numbers. Seasonal
        businesses need year-over-year comparison specifically, never sequential.
      attribution: |
        Lynch, and he'd recognise it instantly. He wrote about walking through
        stores and checking whether inventory was moving; this is the
        filing-level version of the same instinct.
    not_meaningful_when:
      - data: the company reports no inventory
        because: |
          Software, services and most financials carry no inventory, so the
          comparison has nothing to measure.

  - id: receivables_minus_revenue_growth_yoy
    kind: computed
    estimator:
      kind: endpoint
    label: Receivables growth minus revenue growth, year over year
    unit: percentage_points
    format: "+0.0 pp"
    polarity: lower_is_better
    derivation:
      formula: |
        (accounts receivable YoY % change) − (revenue YoY % change)

        Year-over-year specifically, never sequential.
      window: most recent period against the same period one year earlier
    inputs:
      filings:
        - accounts receivable, net
        - revenue
      prices: []
      entries: []
    explanation:
      plain: |
        Whether sales are being booked but not collected. Rising receivables
        outpacing sales means either customers are struggling or the company is
        pushing product into the channel to make a quarter.
      misfires: |
        A genuine shift toward larger enterprise customers with longer payment
        terms will trigger this legitimately. Acquisitions bring receivables
        with them.
      attribution: |
        Lynch. The receivables counterpart to the inventory check.
    not_meaningful_when:
      - industry: [depository-lending]
        because: |
          This asks whether sales are being booked and not collected. A
          lender's receivable is not an uncollected sale — it is the loan,
          which is the asset the business exists to create. Growing it faster
          than income is what lending is, and this would report it as a
          warning.
      - data: the company reports no accounts receivable
        because: |
          The comparison has nothing to measure.

  # ===========================================================================
  # Consistency and history
  # ===========================================================================

  - id: profitable_years_10y
    kind: computed
    estimator:
      kind: count
      observations: 10
    label: Years of positive net income out of the last ten
    unit: count
    format: "0 of 10"
    polarity: higher_is_better
    derivation:
      formula: |
        Count of fiscal years, among the ten most recent, with
        NetIncomeLoss > 0
      window: ten fiscal years, annual observations
    inputs:
      filings:
        - net income (loss)
      prices: []
      entries: []
    explanation:
      plain: |
        How many of the last ten years the company made money. Ten out of ten
        means it has never lost money in a decade — not "usually profitable",
        never lost money.
      misfires: |
        2020 put a non-cash impairment or a shutdown loss on the books of a
        great many otherwise stable companies. Through roughly 2030 any test
        built on this will exclude businesses that were stable before and after.
      attribution: |
        Graham. Stability of earnings, counted rather than averaged.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          A count of profitable years is built on nothing but that figure, so
          a property company earning cash every year can print a loss in
          several of them and read as a business that keeps failing.
      - data: fewer than ten fiscal years of filings are available
        because: |
          A count out of ten cannot be made from a shorter history.

  - id: consecutive_annual_loss_years
    kind: computed
    estimator:
      kind: count
    label: Consecutive annual losses, most recent backward
    unit: count
    format: "0"
    polarity: lower_is_better
    derivation:
      formula: |
        Count of consecutive fiscal years, counting back from the most recent,
        with NetIncomeLoss < 0. Zero if the most recent fiscal year was
        profitable.
      window: consecutive fiscal years, most recent backward
    inputs:
      filings:
        - net income (loss)
      prices: []
      entries: []
    explanation:
      plain: |
        How many years in a row the company has lost money right now. One loss
        year can be an impairment or a settlement; a run of them is the business.
      misfires: |
        A single large non-cash charge — goodwill impairment, pension
        remeasurement — can produce a reported annual loss at a company whose
        operations never stopped generating cash.
      attribution: |
        Graham. The streak form of his earnings-stability test.
    not_meaningful_when:
      - industry: [real-estate]
        because: |
          The accounts charge depreciation against buildings every year as
          though they were wearing out, and well-kept property mostly does
          not. Reported profit is therefore lower than what the business
          actually earns, by a lot, and every measure built on profit inherits
          it. The industry uses a different figure entirely — funds from
          operations, which adds the depreciation back — and this program does
          not compute it.

          A run of reported losses is the thing this counts, and for a
          property company those losses can be the depreciation schedule
          rather than the business — which on a measure where a run is bad
          reads as a company in trouble.

  - id: consecutive_dividend_years
    kind: computed
    estimator:
      kind: count
    label: Consecutive years paying a dividend
    unit: years
    format: "0 yrs"
    polarity: higher_is_better
    derivation:
      formula: |
        Count of consecutive fiscal years, counting back from the most recent,
        with PaymentsOfDividends > 0
      window: consecutive fiscal years, most recent backward
    inputs:
      filings:
        - payments of dividends
      prices: []
      entries: []
    explanation:
      plain: |
        How many years in an unbroken run the company has paid its owners cash.
        A long run means the company has survived a full cycle and returns cash
        to shareholders.
      misfires: |
        This measures one mechanism of returning capital. SEC Rule 10b-18 (1982)
        made buybacks a safe, routine alternative, and a company that returns
        capital consistently through repurchases instead reads as zero here.
      attribution: |
        Graham. His own test used a twenty-year run, written before the
        buyback-versus-dividend choice existed in its modern form.

  - id: consecutive_capital_return_years
    kind: computed
    estimator:
      kind: count
    label: Consecutive years returning capital, by either route
    unit: years
    format: "0 yrs"
    polarity: higher_is_better
    derivation:
      formula: |
        Count of consecutive fiscal years, counting back from the most recent,
        in which EITHER dividends paid > 0
        OR (share repurchases − share issuance) > 0
      window: consecutive fiscal years, most recent backward
    inputs:
      filings:
        - payments of dividends
        - payments for repurchase of common stock
        - proceeds from issuance of common stock
      prices: []
      entries: []
    explanation:
      plain: |
        How many years in an unbroken run the company has handed cash back to
        its owners — by paying a dividend, by buying its own shares back, or
        by both.

        A long run says two things at once, and they are separable. It says
        the company generates more cash than it needs, consistently. And it
        says the company was still there: a run of twenty years reaches back
        through whatever the last two decades contained, which no shorter
        window can evidence at all.

        Buybacks count NET of shares issued. A company handing out more stock
        than it repurchases is not returning capital, it is mopping up after
        its own issuance, and counting the repurchase line gross would let
        that read as a capital return in the year it is least true.
      misfires: |
        Buybacks are discretionary in a way dividends are not. A company can
        repurchase in good years and quietly stop in bad ones without the
        signal a dividend cut sends, so a run counted this way is weaker
        evidence of commitment than the same run of dividends — it evidences
        capacity rather than promise.

        A company that returns capital by special dividend every other year
        breaks the run here even though it has returned a great deal of cash.
      attribution: |
        Graham's twenty-year dividend test, with the mechanism widened. The
        widening is not his: SEC Rule 10b-18 (1982) made repurchases a safe
        and routine alternative to dividends decades after he wrote, and a
        dividend-only count now excludes companies returning capital
        consistently by a route he had no reason to anticipate.
    not_meaningful_when:
      - data: a year in the run paid no dividend and has no resolvable
          repurchase figure
        because: |
          Whether capital was returned that year is unknown, and a streak
          either side of a year nobody can read is not a streak. Stopping the
          count there would assert a break that may not have happened.

  - id: diluted_share_count_change_5y
    kind: computed
    estimator:
      kind: endpoint
    label: Diluted share count change, 5-year
    unit: percent
    format: "+0.0%"
    polarity: lower_is_better
    derivation:
      formula: |
        (diluted weighted shares, latest FY
         ÷ diluted weighted shares, 5 FYs ago) − 1
      window: five fiscal years, endpoint to endpoint
    inputs:
      filings:
        - weighted average number of diluted shares outstanding
      prices: []
      entries: []
    explanation:
      plain: |
        Whether your slice of the company is getting bigger or smaller. A
        company can grow earnings 8% a year and still deliver you nothing if it
        issues 8% more shares a year. Stock compensation is a real expense that
        transfers ownership from you to employees quietly.
      misfires: |
        A single large stock-funded acquisition will blow the window and it may
        have been a fine deal. Post-IPO companies work through lockups and grant
        overhangs for years. Banks that raised equity in a crisis get punished
        by this for half a decade afterward.
      attribution: |
        Buffett, explicit and repetitive: per-share is the only thing that
        matters.

  - id: diluted_share_count_change_3y
    kind: computed
    estimator:
      kind: endpoint
    label: Diluted share count change, 3-year
    unit: percent
    format: "+0.0%"
    polarity: lower_is_better
    derivation:
      formula: |
        (diluted weighted shares, latest FY
         ÷ diluted weighted shares, 3 FYs ago) − 1
      window: three fiscal years, endpoint to endpoint
    inputs:
      filings:
        - weighted average number of diluted shares outstanding
      prices: []
      entries: []
    explanation:
      plain: |
        The same question as the five-year measure over a shorter run: whether
        your slice of the company has shrunk or been diluted recently. The
        shorter window catches a change in management's behaviour rather than
        its long-run habit.
      misfires: |
        Same as the five-year measure, and more exposed to a single stock-funded
        acquisition because there are fewer years to absorb it.
      attribution: |
        Buffett. The recency counterpart to
        [diluted_share_count_change_5y].

  - id: diluted_share_count_change_ttm
    kind: computed
    estimator:
      kind: endpoint
    label: Diluted share count change, trailing twelve months
    unit: percent
    format: "+0.0%"
    polarity: lower_is_better
    derivation:
      formula: |
        (diluted weighted shares, TTM
         ÷ diluted weighted shares, prior year TTM) − 1
      window: trailing twelve months against the prior trailing twelve months
    inputs:
      filings:
        - weighted average number of diluted shares outstanding
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the company has issued or retired shares in the last year. A
        company that funds itself by issuing stock into a depressed price is
        transferring value from existing owners to new ones.
      misfires: |
        A convertible issue, a stock-funded deal, or an at-the-market programme
        all read the same here. The measure says shares were issued, never why.
      attribution: |
        Buffett's per-share discipline, on the shortest available window.

  - id: goodwill_intangibles_to_assets
    kind: computed
    estimator:
      kind: instant
    label: Goodwill plus intangibles over total assets
    unit: percent
    format: "0.0%"
    polarity: none
    polarity_note: |
      Deliberately none. A high figure describes a company that was assembled by
      acquisition rather than built; whether that is a strength or a weakness is
      a judgement this file does not make.
    derivation:
      formula: |
        (goodwill + intangible assets) ÷ total assets
      window: most recent balance sheet
    inputs:
      filings:
        - goodwill
        - intangible assets excluding goodwill
        - total assets
      prices: []
      entries: []
    explanation:
      plain: |
        How much of the company was bought rather than built. Goodwill is the
        premium paid over the fair value of things acquired; it represents
        returns purchased with shareholder money rather than generated by
        operations.
      misfires: |
        Genuinely good acquisitive compounders read high on this and are not
        thereby worse businesses. A goodwill impairment is an event rather than
        a level, and this measure only ever shows the level.
      attribution: |
        Buffett's preference for return on unleveraged net *tangible* assets,
        expressed as a balance-sheet composition.
    not_meaningful_when:
      - industry: [depository-lending, insurance]
        because: |
          This asks how much of the company was bought rather than built, by
          weighing goodwill against everything the company owns. For a lender
          what it owns is the loan book — hundreds of billions of it — so a
          bank assembled entirely out of acquisitions reads at two or three
          per cent, the same as one that has never bought anything. The number
          is not answering the question it states.

  - id: goodwill_impairment_to_equity_5y
    kind: computed
    estimator:
      kind: cumulative
      observations: 5
    label: Goodwill written off over five years, against opening equity
    unit: percent
    format: "0.0%"
    polarity: lower_is_better
    derivation:
      formula: |
        sum( goodwill impairment charged in each of the last 5 fiscal years )
        ÷ total shareholders' equity at the year end BEFORE the window opened
      window: five fiscal years of charges, over the balance sheet that
        preceded them
    inputs:
      filings:
        - goodwill impairment charge
        - total shareholders' equity
      prices: []
      entries: []
    explanation:
      plain: |
        How much of what the company paid for other businesses it has since
        admitted was not there — measured against the owners' money that
        existed before the write-offs began.

        Goodwill is the premium an acquirer pays over what the acquired
        company's identifiable assets are worth. Writing it off is management
        conceding, in public and in the accounts, that the premium bought
        nothing. One write-off is a deal that went wrong. A run of them
        against a large share of the equity base is a company whose growth has
        been purchased at prices it could not justify.

        The denominator is the equity BEFORE the window rather than after it,
        and that is deliberate: impairments reduce equity, so measuring
        against today's would divide a large numerator by a denominator the
        same charges had already shrunk, and flatter the worst cases most.
      misfires: |
        Impairment testing is annual and judgement-heavy, so the timing says
        more about when management conceded than about when the value went.
        Two companies whose acquisitions failed identically can be years apart
        here.

        A write-off can also follow a change in interest rates rather than in
        the business — discount rates feed the impairment test directly — so a
        charge in a year rates moved sharply is weaker evidence than a charge
        in a quiet one.

        A company that has never acquired anything reads zero, correctly, and
        so does one whose acquisitions have all worked. The measure cannot
        tell those apart and is not trying to.
      attribution: |
        Not Buffett's, and pointedly the opposite of a related test he would
        object to. His 1983 letter appendix argues that economic goodwill is
        the most valuable asset a business owns and that accounting goodwill
        measures it poorly — which is an argument against judging a company by
        how much goodwill sits on its balance sheet, and no argument at all
        against judging it by how much it has had to write back off.
    not_meaningful_when:
      - data: shareholders' equity before the window is zero or negative
        because: |
          There is no base to measure the write-offs against.
      - data: any year in the window has no resolvable impairment figure
        because: |
          A missing caption is not evidence of no write-off. A filer reporting
          one combined goodwill-and-intangibles line, and nothing this map
          reads, would show nothing in a year it wrote off billions — and a
          zero there is a large confident number in the direction that
          flatters the company.

  # ===========================================================================
  # Ownership and insider behaviour
  # ===========================================================================

  - id: insider_net_buying_6m
    kind: computed
    estimator:
      kind: trailing
    label: Insider net buying, trailing six months
    unit: shares
    format: "+0,0 sh"
    polarity: higher_is_better
    polarity_note: |
      Asymmetric on purpose. Net buying is favourable; net selling is
      uninformative rather than unfavourable — insiders sell for dozens of
      reasons that have nothing to do with the business, but they buy for one.
    derivation:
      formula: |
        Net shares purchased across all Form 4 filings for the issuer over the
        last 180 days, excluding option exercises and transactions coded as
        10b5-1 plan sales.
      window: trailing 180 days
    inputs:
      filings:
        - Form 4 ownership reports (XML, not XBRL company facts)
      prices: []
      entries: []
    explanation:
      plain: |
        Whether the people who know the most about the company are putting their
        own money in.
      misfires: |
        Small purchases timed for optics. Option exercises misparsed as
        purchases. Requires parsing Form 4 XML rather than XBRL company facts,
        so it is a separate ingestion path from every other entry in this file.
      attribution: |
        Lynch. It is also the only measure available here that updates in days
        rather than quarters.
    not_meaningful_when:
      - data: no Form 4 filings exist for the issuer in the window
        because: |
          Silence is not the same as an absence of buying; there is nothing to
          report either way.

  - id: institutional_ownership_pct
    kind: computed
    estimator:
      kind: instant
    label: Institutional ownership
    unit: percent
    format: "0.0%"
    polarity: none
    polarity_note: |
      Deliberately none. High institutional ownership reads as validation to one
      investor and as an absence of any marginal buyer left to another. Both
      readings are held by real strategies.
    derivation:
      formula: |
        Sum of shares reported across 13F filings for the ticker
        ÷ shares outstanding
      window: most recent 13F reporting quarter
    inputs:
      filings:
        - 13F holdings reports (aggregated across filers)
        - common shares outstanding
      prices: []
      entries: []
    explanation:
      plain: |
        What share of the company is held by professional fund managers rather
        than by individuals.
      misfires: |
        13F data lags 45 days, aggregating it across hundreds of filers per
        quarter is heavy, and the resulting number is approximate at best.
      attribution: |
        Lynch, who wanted this to be low: a stock already owned by every large
        fund has no marginal buyer left, and the best opportunities are in
        companies Wall Street hasn't covered yet.
    not_meaningful_when:
      - data: 13F holdings have not been aggregated for the ticker
        because: |
          A partial sum understates ownership without saying by how much.

  # ===========================================================================
  # Qualitative fields
  #
  # Not computed. The user writes their reasoning in prose and marks the field
  # pass or fail. Unmarked is a third state — it is not a fail, and it is not a
  # pass.
  #
  # These entries define the question. They never say what a good answer is.
  #
  # Answered per security, in the journal, and answerable from here alone: the
  # question, the plain-language explanation, where it misfires and whose idea
  # it was are what someone assessing their first moat has to read, and they
  # belong beside the definition rather than restated inside every strategy
  # that asks. A strategy cites one exactly as it cites any other measure and
  # the host answers; nothing about the shape of an answer is a strategy's to
  # invent. The record of answers is append-only and dated — see
  # engine/judgements.py.
  # ===========================================================================

  - id: moat_durability
    kind: qualitative
    estimator:
      kind: assessed
    label: Moat durability
    unit: yes_no
    question: |
      What stops a competitor from taking this business's customers or its
      margin over the next ten years, and will that still be true in ten years?
    response:
      prose: required
      marks: [pass, fail]
      unmarked: unassessed
    explanation:
      plain: |
        A moat is whatever makes it hard or unprofitable for someone else to do
        what this company does — a brand people ask for by name, a network that
        gets more useful as it grows, a cost position nobody can match, a
        switching cost customers won't pay. The question is not whether the
        company is winning now. It is whether the reason it is winning will
        still hold.
      misfires: |
        Nothing about this is recoverable from filings. It is also the field
        most vulnerable to being written after the decision rather than before
        it: a moat can be argued for almost any company if the answer is already
        known.
      attribution: |
        Buffett. He would say this, and not any ratio, is most of the job.

  - id: management_integrity
    kind: qualitative
    estimator:
      kind: assessed
    label: Management integrity
    unit: yes_no
    question: |
      Has management told shareholders the truth, including when the news was
      bad, and does what they say in the letter match what shows up in the
      filings?
    response:
      prose: required
      marks: [pass, fail]
      unmarked: unassessed
    explanation:
      plain: |
        Whether the people running the company can be taken at their word. The
        evidence is in the record: promises made and then kept or quietly
        dropped, bad quarters explained or buried, accounting that gets more
        aggressive when results get worse.
      misfires: |
        Not tagged anywhere and not computable. Partial evidence is recoverable
        from DEF 14A compensation structures and from Form 4 insider behaviour,
        but neither is a substitute for reading what management wrote and
        comparing it with what happened.
      attribution: |
        Buffett. One of the qualities the data constraint of this tool cannot
        reach.

  - id: capital_allocation
    kind: qualitative
    estimator:
      kind: assessed
    label: Capital allocation
    unit: yes_no
    question: |
      When this company has spare cash, where has it gone — and did those
      decisions turn out to be worth more than paying it out would have been?
    response:
      prose: required
      marks: [pass, fail]
      unmarked: unassessed
    explanation:
      plain: |
        Every year a company decides what to do with the money it earns:
        reinvest it, buy something, pay down debt, buy back stock, or hand it
        over. Done well this compounds; done badly it is the main way good
        businesses become ordinary ones. The distinction to look for is whether
        the chief executive behaves like an allocator of capital or like an
        empire builder.
      misfires: |
        Not computable. The quantitative traces — goodwill on the balance sheet,
        share count, payout — say what was done, never whether it was worth
        doing.
      attribution: |
        Buffett. Named in the report as one of the things the data constraint
        costs, alongside moat durability and management integrity.
