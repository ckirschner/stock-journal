# Personal data never enters the repository.
data/
*.json.broken
*.json.tmp
ledger-backup-*.json
pre-import-*.json

# Reference material is local only — it vendors a whole repo and a venv.
# The retired rulesets are the exception: they are the input to authoring
# the real strategies, so they have to survive a fresh clone.
dev_reference_docs/*
!dev_reference_docs/legacy-profiles/

# Agent context — local only.
claude.md
CLAUDE.md
.claude/

__pycache__/
*.py[cod]
build/
dist/
*.spec
.venv/
venv/

.DS_Store
